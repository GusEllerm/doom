/**
 * sim/map — runtime map setup (M2-04, p_setup equivalent). STUB.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */

import type { MapData } from '../wad/types';

/** Stub: filled in by M2-04. */
export function buildMapFromData(_md: MapData): never {
  throw new Error('not implemented');
}
