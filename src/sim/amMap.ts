// sim/amMap.ts — automap state machine (am_map.c port; ARCHITECTURE §5.4,
// M2-plan §M2-08, R09 §5). Stub — implementation lands in the next commit.
//
// SPDX-License-Identifier: GPL-2.0-or-later

export interface AutomapState {
  automapactive: boolean;
}
