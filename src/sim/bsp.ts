/**
 * sim/bsp — BSP point-side test + subsector location (M2-05).
 * STUB — implementation lands in the next commit.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */

import type { RuntimeMap } from './map';

/** Walk the node tree to the subsector containing fixed point (x,y). */
export function subsectorAt(_map: RuntimeMap, _x: number, _y: number): number {
  throw new Error('not implemented');
}
