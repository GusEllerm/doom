// sim/amMap tests (M2-08 — automap state machine). Stub.
//
// SPDX-License-Identifier: GPL-2.0-or-later
import { describe, expect, it } from 'vitest';

describe('amMap stub', () => {
  it('exists', () => {
    expect(1).toBe(1);
  });
});
