/**
 * M7-06: powerups (STUB — implementation in progress).
 *
 * Mirrors: p_inter.c (P_GivePower), p_user.c (P_PlayerThink powers decay),
 * r_data.c / r_main.c (playpal bands), d_player.h (POWER_ enums).
 */
export const M7_06_STUB = true;
