// audio/smf.test.ts — M10-03 acceptance goldens (hand-built fixtures only).
// STUB — goldens land with the implementation.
//
// SPDX-License-Identifier: GPL-2.0-or-later

import { describe, it, expect } from 'vitest';

describe('smf stub', () => {
  it('placeholder', () => {
    expect(1).toBe(1);
  });
});
