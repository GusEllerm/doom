// input/keyboard.ts — DOM keyboard → held-input state → GameInput sampling
// (M2-07 stub; implementation follows in the same branch).
// SPDX-License-Identifier: GPL-2.0-or-later

export function createKeyboardInput(): never {
  throw new Error('M2-07 stub');
}
