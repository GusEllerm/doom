/**
 * M4-03 tests — static thing/sprite tables.
 *
 * STUB — tests in progress.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */

import { describe, it, expect } from 'vitest';

describe('rthings (stub)', () => {
  it('loads', async () => {
    const mod = await import('./rthings');
    expect(mod.M4_03_STUB).toBe(true);
  });
});
