// render/drawsegs.ts — drawseg ledger + openings pool + masked-pass stubs
// (M3-plan §M3-06; r_segs.c + r_defs.h drawseg_s). STUB — implemented in
// this task's second commit.
//
// SPDX-License-Identifier: GPL-2.0-or-later

/** r_defs.h:55 */
export const MAXDRAWSEGS = 256;

// TODO(M3-06): SoA ledger, openings pool, clip arrays, stubs.
export function clearDrawsegs(): void {
  // TODO
}
