// render/masked.ts — R_RenderMaskedSegRange + shared R_DrawMaskedColumn
// (M4-plan §M4-04; linuxdoom-1.10/r_segs.c:96-176 + r_draw.c R_DrawMaskedColumn).
//
// STUB (M4-04 scaffold) — implementations land in this task's follow-up commits.
//
// SPDX-License-Identifier: GPL-2.0-or-later

/**
 * R_RenderMaskedSegRange (r_segs.c:100-190) — draws the masked middle of
 * drawseg `ds` between columns [x1, x2], consuming + resetting the recorded
 * maskedtexturecol entries. Stub until implemented in M4-04.
 */
export function renderMaskedSegRange(ds: number, x1: number, x2: number): void {
  void ds;
  void x1;
  void x2;
}
