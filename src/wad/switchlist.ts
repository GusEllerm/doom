// wad/switchlist.ts — P_InitSwitchList data (p_switch.c alphSwitchList,
// M6-11). STUB (checkpoint commit) — full port lands in this commit series.
//
// SPDX-License-Identifier: GPL-2.0-or-later

/** Placeholder — see implementation below in this branch. */
export function buildSwitchList(
  textureNames: readonly string[], episode = 2
): string[] {
  void textureNames;
  void episode;
  return [];
}
