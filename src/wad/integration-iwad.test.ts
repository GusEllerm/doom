/**
 * M1-09 — IWAD integration goldens: one describe.skipIf(!hasWad) block
 * consolidating the cross-decoder end-to-end evidence over freedoom1.wad
 * (palette/colormap hashes, PNAMES/TEXTURE counts, sprite census totals,
 * one patch + one composed-texture sha256).
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */
export {};
