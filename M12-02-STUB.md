# M12-02 STUB — Every-map L3 render corpus + montage

Claimed. Owns: tests/render/viewpointsAllMaps.ts, tests/render/allMaps.test.ts,
goldens set maps/, docs/reports/M12-montage.md, goldens-update register (maps set).
Plan: docs/design/M12-plan.md §M12-02. ≥8 analytically-derived viewpoints x 9 E1
maps (census-driven, no screenshot peeking), HOM==0 + anomaly counters per frame,
indices-hash goldens set 'maps' + PNG companions + montage contact sheet (D016).
