# M7 random-sites ledger (P_Random call-site table) — STUB

M7-09 in progress. Plan ownership says `docs/design/random-sites.md`; the
orchestrator brief forbids this branch from touching `docs/**`, so the file
lives at the repo root and the orchestrator moves it on merge.

(Table to be filled: action → calls-per-invocation, feeds ARCH §5.2.)
