/**
 * e2e M9-12 — L4: THE WHOLE LOOP with real keys/mouse (M9-plan §M9-12).
 *
 * STUB (first commit): the flow battery lands incrementally:
 *   TITLEPIC boot → real Esc → real arrows/Enter skill → play → mouse-menu
 *   (menuMouse synth) → Options thermo/resize → statusbar face probe →
 *   scripted exit → intermission accel-click → E1M2 → death → faithful
 *   restart → F7 endgame-yes → title.
 *
 * SPDX-License-Identifier: GPL-2.0-or-later
 */
import { expect, test } from '@playwright/test';

test.describe('M9-12 whole-loop (stub)', () => {
  test('stub placeholder — flow battery WIP', async () => {
    expect(true).toBe(true);
  });
});
