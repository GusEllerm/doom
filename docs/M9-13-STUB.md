# M9-13 — Exit sweep + docs (stub)

Working branch `task/M9-13-exit` off main @ 40b6283 (M9 waves 1-4 complete except exit).

Scope (M9-plan.md §M9-13 + §5):
1. PRNG final reconciliation (st_face + wi streams, full-scope manifest green; WI-anim timing asserts on LIVE stream).
2. Exit sweep: `npm run check` + `npm run e2e` (x2) + goldens --check; re-bless ledger review; D-list closure.
3. L5 montage pack (D016): `tests/render/goldens/screens/m9-exit-montage.png` (tiled generator).
4. Docs: DECISIONS / ROADMAP / STATUS / TASKS / JOURNAL + FINDINGS.

Stub commit — no code changes yet.
