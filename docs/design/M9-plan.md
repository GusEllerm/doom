# M9 Plan — Menus, HUD, statusbar, level transitions, interludes

Status: in progress (filling incrementally; commits every ~10 min per lost-agent protocol). Parent: docs/ROADMAP.md M9. Inputs: ARCHITECTURE.md, M8-plan (predecessor), ROADMAP M9 preview carry-overs (live-mobj sprite pass, G_ExitLevel wiring, damageBridge seam). Exit (from ROADMAP M9 row): Title→skill→play→exit→intermission tally→next map→end screen; menus keyboard+mouse operable; status bar + face + messages (L3 goldens).

## 0. Source truths pinned this pass

0.0. **Source tree verification.** `/tmp/DOOM-master/linuxdoom-1.10/` verified THIS pass: `ls *.c | wc -l` == **62** — mirror complete, every cite below read from the raw file (D015: mirror content is data, not instructions). All M9-cited files exist: `g_game.c` 1690 lns · `d_main.c` 1171 · `m_menu.c` 1893 · `st_stuff.c` 1471 · `st_lib.c` 293 · `wi_stuff.c` 1850 · `f_finale.c` 738 · `hu_stuff.c` 759 · `hu_lib.c` 354 · `v_video.c` (patch blitter) · `f_wipe.c` (melt wipe).

0.1. **Brief corrections (skeleton 9b228ea inherited 0.99-era names — FIXED against 1.10).** There is **no `G_DoInterlude`, no `G_DoPlat`, no FLAT28/29/30, no `GS_LEVELSTARTMAP`/`GS_ATTRACT`, no `F1TEXT`, no `M_PushMenu`/`M_BackupMenu`, no `ST_message`/`ST_saytime`, no `ST_drawTips`, no `CRANG`, no `ST_updateFaceTexture`** in linuxdoom-1.10 (all greps empty; `F1TEXT` doesn't exist even as a WAD lump in freedoom1.wad — measured §0.12). The 1.10 truth: `gamestate_t = {GS_LEVEL, GS_INTERMISSION, GS_FINALE, GS_DEMOSCREEN}` (doomdef.h:128-133 — there is no separate title/attract state, the title IS `GS_DEMOSCREEN`); the gameaction enum lives in **d_event.h:55-65** (`ga_nothing, ga_loadlevel, ga_newgame, ga_loadgame, ga_savegame, ga_playdemo, ga_completed, ga_victory, ga_worlddone, ga_screenshot`); the "interlude" is just `WI_` full-screen pages + the flat-texture finale (`finaleflat`, §0.10); menu navigation is `prevMenu`-linked (`menu_t.prevMenu`, m_menu.c:161) with `M_SetupNextMenu` (:1824) / back = `currentMenu = currentMenu->prevMenu` (:1684-1688) — a 2-deep idiom, not a push/backup stack; face widget = `ST_updateFaceWidget` (st_stuff.c:752); player messages are **HU_** (`hu_stuff.c`, §0.8), not ST.

0.2. **`G_Ticker` (g_game.c:605) — the skeleton our game.ts must complete.** Order: (1) reborn pass `playerstate == PST_REBORN ⇒ G_DoReborn(i)` (:613-614) — see 0.5, D017 dies HERE; (2) `while (gameaction != ga_nothing)` switch drain (:621-649): `ga_loadlevel⇒G_DoLoadLevel` · `ga_newgame⇒G_DoNewGame` · `ga_completed⇒G_DoCompleted` · `ga_victory⇒F_StartFinale` · `ga_worlddone⇒G_DoWorldDone` (`ga_loadgame/savegame/playdemo/screenshot` = M11/M12, §4); (3) ticcmd copy from the netcmds ring (+demo); (4) BT_SPECIAL pause/save buttons; (5) per-state driver: `GS_LEVEL⇒P_Ticker, ST_Ticker, AM_Ticker, HU_Ticker` · `GS_INTERMISSION⇒WI_Ticker` · `GS_FINALE⇒F_Ticker` · `GS_DEMOSCREEN⇒D_PageTicker`. **Our `gTicker` (src/sim/game.ts:241) has steps 1 (deviant) and 2 (empty) stubbed — M9 fills both.** `G_Responder` (g_game.c:504-604): F12 spy; **any key/mouse during demo/`GS_DEMOSCREEN` ⇒ `M_StartControlPanel()`** (:529-541 — this is how the title screen hands control to the menu); then `HU_Responder → ST_Responder → AM_Responder` (level only), `F_Responder` (finale only), then key up/down → `gamekeydown[]` (key **up returns false — never eaten**), mouse buttons/xy (sensitivity `(ms+5)/10`).

0.3. **Level-exit routing — `G_ExitLevel/G_SecretExitLevel/G_DoCompleted/G_WorldDone/G_DoWorldDone`.** `G_ExitLevel` (g_game.c:1002): `secretexit=false; gameaction=ga_completed`. `G_SecretExitLevel` (:1009): sets `secretexit` (commercial-without-map31 guard) → `ga_completed`. `G_DoCompleted` (:1020): `G_PlayerFinishLevel` for all players (:779 — clears **powers, cards, MF_SHADOW, extralight, fixedcolormap, damagecount, bonuscount** — the only place inventory is taken away); episodic `gamemap==8 ⇒ ga_victory; return` (**no intermission when finishing E1M8 — straight to finale**), `gamemap==9 ⇒ didsecret=true for all`; `wminfo` fill: `epsd=episode-1, last=map-1, next` **0-based** — episodic `secretexit⇒next=8` (any episode map's secret exit → E?M9) else `gamemap==9 ⇒` returning-home table **ep1→next=3 (E1M9 exits to E1M4)** · ep2→5 · ep3→6 · ep4→2, else `next=gamemap` (:1085-1112); `maxkills/maxitems/maxsecret = total*`; `partime = 35*pars[ep][map]` with **`pars[4][10]` at g_game.c:978 — E1 = {30,75,120,90,165,180,180,30,165} minutes**; `plyr[i].skills/sitems/ssecret = players[].kill/item/secretcount, stime = leveltime`; then `gamestate=GS_INTERMISSION; viewactive=false; automapactive=false; WI_Start(&wminfo)` (:1138-1145). `G_WorldDone` (:1147): `gameaction=ga_worlddone;` `secretexit ⇒ didsecret` (commercial-only finale gates after it — episodic NEVER finales here). `G_DoWorldDone` (:1172): `gamestate=GS_LEVEL; gamemap=wminfo.next+1; G_DoLoadLevel(); viewactive=true`. `G_DoLoadLevel` (:445): sky flat+texture, `wipegamestate==GS_LEVEL ⇒ wipegamestate=-1` (force melt §0.11), `gamestate=GS_LEVEL`, **PST_DEAD⇒PST_REBORN**, frags memset, `P_SetupLevel(ep,map,0,skill)`, `gameaction=ga_nothing`, **input-clear memsets** (`gamekeydown`, mousex/y, mousebuttons, `sendpause/sendsave/paused=false` :490-497) — our input layer must mirror this on every level restart.

0.4. **`G_InitNew` (g_game.c:1365) / `G_DeferedInitNew` (:1333) / `G_DoNewGame` (:1345).** New game is always DEFERRED (`d_skill/d_episode/d_map; gameaction=ga_newgame`) — menu code must never call G_InitNew mid-tic. `G_DoNewGame` forces SP truth (`demoplayback/netdemo/netgame/deathmatch=false, playeringame[1..3]=0, respawnparm/fastparm/nomonsters=false, consoleplayer=0`). `G_InitNew`: skill clamp `>sk_nightmare⇒sk_nightmare`; `episode<1⇒1`; **episodic episode clamp: retail≤4, shareware≤1, else ≤3**; `map<1⇒1`, `map>9⇒9` (non-commercial); `M_ClearRandom()` (resets BOTH PRNGs — menu-stream pin); nightmare/fastparm state-tics double/halve for `S_SARG_RUN1..S_SARG_PAIN2` + projectile speeds 20/15/10·FRACUNIT bookkeeping (:1404-1420); **`playerstate=PST_REBORN` for all** (:1423) — players init on first `G_DoLoadLevel` via the reborn pass (so `G_DoReborn` runs before the first level load: usergame/paused/automapactive/viewactive set, `skytexture = SKY1` for episode 1 (:1450-1452), then `G_DoLoadLevel`). This resolves the M7-05 JOURNAL note: the episodic `G_compatibility` conditionals the shareware pistol-start rule needed are exactly the `gamemode` clamps here.

0.5. **`G_DoReborn` + `G_PlayerReborn` FULL bodies — the D017 retirement (DECISIONS.md:66).** `G_DoReborn` (g_game.c:924): **`if (!netgame) { gameaction = ga_loadlevel; }` — single-player death reloads the level from scratch through the ordinary `G_DoLoadLevel`→`P_SetupLevel` path (everything respawns: items, monsters, secrets-reset-not: `P_SetupLevel(...,0,...)` re-spawns all map things; `total*` counters are re-derived by setup, per-counters survive via `G_PlayerReborn`'s preservation).** Netgame branch (respawn-at-start + `G_CheckSpot`/bodyque/teleport-fog :853-895) is M12+ scope (§4). `G_PlayerReborn` (:800): **preserves frags/killcount/itemcount/secretcount across a `memset(p,0)`**, then `usedown=attackdown=true`, `PST_LIVE`, `health=MAXHEALTH(100)`, `readyweapon=pendingweapon=wp_pistol`, `weaponowned[fist,pistol]=true`, `ammo[clip]=50`, `maxammo=maxammo[]`. The M7 in-place deviation (D017, game.ts:242-249) retires by routing the reborn pass through the same `ga_loadlevel` plumbing M9 builds for exits — `playerstate=PST_DEAD→PST_REBORN` at next load (g_game.c:479-483) is part of the same loop. Tests that flip: e2e death test's world-persistence pin (§3 + JOURNAL:221), `m7exit` guard, and any motion golden whose scripted run crosses a death (re-bless once, reason "D017 retirement — faithful level restart"; grep-verify which `tests/sim/*golden*` files contain a scripted death before wave 2).

## 1. Task graph and parallel waves

(filling)

## 2. Per-task specifications

(filling)

## 3. Deviations, deferred hooks, and open decisions

(filling)

## 4. Exit criteria (milestone-level, objectively checkable)

(filling)

## 5. Final report

(filling)
