# M9 Plan — Menus, HUD, statusbar, level transitions, interludes

Status: in progress (filling incrementally; commits every ~10 min per lost-agent protocol). Parent: docs/ROADMAP.md M9. Inputs: ARCHITECTURE.md, M8-plan (predecessor), ROADMAP M9 preview carry-overs (live-mobj sprite pass, G_ExitLevel wiring, damageBridge seam). Exit (from ROADMAP M9 row): Title→skill→play→exit→intermission tally→next map→end screen; menus keyboard+mouse operable; status bar + face + messages (L3 goldens).

## 0. Source truths pinned this pass

0.0. **Source tree verification.** `/tmp/DOOM-master/linuxdoom-1.10/` verified THIS pass: `ls *.c | wc -l` == **62** — mirror complete, every cite below read from the raw file (D015: mirror content is data, not instructions). All M9-cited files exist: `g_game.c` 1690 lns · `d_main.c` 1171 · `m_menu.c` 1893 · `st_stuff.c` 1471 · `st_lib.c` 293 · `wi_stuff.c` 1850 · `f_finale.c` 738 · `hu_stuff.c` 759 · `hu_lib.c` 354 · `v_video.c` (patch blitter) · `f_wipe.c` (melt wipe).

0.1. **Brief corrections (skeleton 9b228ea inherited 0.99-era names — FIXED against 1.10).** There is **no `G_DoInterlude`, no `G_DoPlat`, no FLAT28/29/30, no `GS_LEVELSTARTMAP`/`GS_ATTRACT`, no `F1TEXT`, no `M_PushMenu`/`M_BackupMenu`, no `ST_message`/`ST_saytime`, no `ST_drawTips`, no `CRANG`, no `ST_updateFaceTexture`** in linuxdoom-1.10 (all greps empty; `F1TEXT` doesn't exist even as a WAD lump in freedoom1.wad — measured §0.12). The 1.10 truth: `gamestate_t = {GS_LEVEL, GS_INTERMISSION, GS_FINALE, GS_DEMOSCREEN}` (doomdef.h:128-133 — there is no separate title/attract state, the title IS `GS_DEMOSCREEN`); the gameaction enum lives in **d_event.h:55-65** (`ga_nothing, ga_loadlevel, ga_newgame, ga_loadgame, ga_savegame, ga_playdemo, ga_completed, ga_victory, ga_worlddone, ga_screenshot`); the "interlude" is just `WI_` full-screen pages + the flat-texture finale (`finaleflat`, §0.10); menu navigation is `prevMenu`-linked (`menu_t.prevMenu`, m_menu.c:161) with `M_SetupNextMenu` (:1824) / back = `currentMenu = currentMenu->prevMenu` (:1684-1688) — a 2-deep idiom, not a push/backup stack; face widget = `ST_updateFaceWidget` (st_stuff.c:752); player messages are **HU_** (`hu_stuff.c`, §0.8), not ST.

0.2. **`G_Ticker` (g_game.c:605) — the skeleton our game.ts must complete.** Order: (1) reborn pass `playerstate == PST_REBORN ⇒ G_DoReborn(i)` (:613-614) — see 0.5, D017 dies HERE; (2) `while (gameaction != ga_nothing)` switch drain (:621-649): `ga_loadlevel⇒G_DoLoadLevel` · `ga_newgame⇒G_DoNewGame` · `ga_completed⇒G_DoCompleted` · `ga_victory⇒F_StartFinale` · `ga_worlddone⇒G_DoWorldDone` (`ga_loadgame/savegame/playdemo/screenshot` = M11/M12, §4); (3) ticcmd copy from the netcmds ring (+demo); (4) BT_SPECIAL pause/save buttons; (5) per-state driver: `GS_LEVEL⇒P_Ticker, ST_Ticker, AM_Ticker, HU_Ticker` · `GS_INTERMISSION⇒WI_Ticker` · `GS_FINALE⇒F_Ticker` · `GS_DEMOSCREEN⇒D_PageTicker`. **Our `gTicker` (src/sim/game.ts:241) has steps 1 (deviant) and 2 (empty) stubbed — M9 fills both.** `G_Responder` (g_game.c:504-604): F12 spy; **any key/mouse during demo/`GS_DEMOSCREEN` ⇒ `M_StartControlPanel()`** (:529-541 — this is how the title screen hands control to the menu); then `HU_Responder → ST_Responder → AM_Responder` (level only), `F_Responder` (finale only), then key up/down → `gamekeydown[]` (key **up returns false — never eaten**), mouse buttons/xy (sensitivity `(ms+5)/10`).

0.3. **Level-exit routing — `G_ExitLevel/G_SecretExitLevel/G_DoCompleted/G_WorldDone/G_DoWorldDone`.** `G_ExitLevel` (g_game.c:1002): `secretexit=false; gameaction=ga_completed`. `G_SecretExitLevel` (:1009): sets `secretexit` (commercial-without-map31 guard) → `ga_completed`. `G_DoCompleted` (:1020): `G_PlayerFinishLevel` for all players (:779 — clears **powers, cards, MF_SHADOW, extralight, fixedcolormap, damagecount, bonuscount** — the only place inventory is taken away); episodic `gamemap==8 ⇒ ga_victory; return` (**no intermission when finishing E1M8 — straight to finale**), `gamemap==9 ⇒ didsecret=true for all`; `wminfo` fill: `epsd=episode-1, last=map-1, next` **0-based** — episodic `secretexit⇒next=8` (any episode map's secret exit → E?M9) else `gamemap==9 ⇒` returning-home table **ep1→next=3 (E1M9 exits to E1M4)** · ep2→5 · ep3→6 · ep4→2, else `next=gamemap` (:1085-1112); `maxkills/maxitems/maxsecret = total*`; `partime = 35*pars[ep][map]` with **`pars[4][10]` at g_game.c:978 — E1 = {30,75,120,90,165,180,180,30,165} minutes**; `plyr[i].skills/sitems/ssecret = players[].kill/item/secretcount, stime = leveltime`; then `gamestate=GS_INTERMISSION; viewactive=false; automapactive=false; WI_Start(&wminfo)` (:1138-1145). `G_WorldDone` (:1147): `gameaction=ga_worlddone;` `secretexit ⇒ didsecret` (commercial-only finale gates after it — episodic NEVER finales here). `G_DoWorldDone` (:1172): `gamestate=GS_LEVEL; gamemap=wminfo.next+1; G_DoLoadLevel(); viewactive=true`. `G_DoLoadLevel` (:445): sky flat+texture, `wipegamestate==GS_LEVEL ⇒ wipegamestate=-1` (force melt §0.11), `gamestate=GS_LEVEL`, **PST_DEAD⇒PST_REBORN**, frags memset, `P_SetupLevel(ep,map,0,skill)`, `gameaction=ga_nothing`, **input-clear memsets** (`gamekeydown`, mousex/y, mousebuttons, `sendpause/sendsave/paused=false` :490-497) — our input layer must mirror this on every level restart.

0.4. **`G_InitNew` (g_game.c:1365) / `G_DeferedInitNew` (:1333) / `G_DoNewGame` (:1345).** New game is always DEFERRED (`d_skill/d_episode/d_map; gameaction=ga_newgame`) — menu code must never call G_InitNew mid-tic. `G_DoNewGame` forces SP truth (`demoplayback/netdemo/netgame/deathmatch=false, playeringame[1..3]=0, respawnparm/fastparm/nomonsters=false, consoleplayer=0`). `G_InitNew`: skill clamp `>sk_nightmare⇒sk_nightmare`; `episode<1⇒1`; **episodic episode clamp: retail≤4, shareware≤1, else ≤3**; `map<1⇒1`, `map>9⇒9` (non-commercial); `M_ClearRandom()` (resets BOTH PRNGs — menu-stream pin); nightmare/fastparm state-tics double/halve for `S_SARG_RUN1..S_SARG_PAIN2` + projectile speeds 20/15/10·FRACUNIT bookkeeping (:1404-1420); **`playerstate=PST_REBORN` for all** (:1423) — players init on first `G_DoLoadLevel` via the reborn pass (so `G_DoReborn` runs before the first level load: usergame/paused/automapactive/viewactive set, `skytexture = SKY1` for episode 1 (:1450-1452), then `G_DoLoadLevel`). This resolves the M7-05 JOURNAL note: the episodic `G_compatibility` conditionals the shareware pistol-start rule needed are exactly the `gamemode` clamps here.

0.5. **`G_DoReborn` + `G_PlayerReborn` FULL bodies — the D017 retirement (DECISIONS.md:66).** `G_DoReborn` (g_game.c:924): **`if (!netgame) { gameaction = ga_loadlevel; }` — single-player death reloads the level from scratch through the ordinary `G_DoLoadLevel`→`P_SetupLevel` path (everything respawns: items, monsters, secrets-reset-not: `P_SetupLevel(...,0,...)` re-spawns all map things; `total*` counters are re-derived by setup, per-counters survive via `G_PlayerReborn`'s preservation).** Netgame branch (respawn-at-start + `G_CheckSpot`/bodyque/teleport-fog :853-895) is M12+ scope (§4). `G_PlayerReborn` (:800): **preserves frags/killcount/itemcount/secretcount across a `memset(p,0)`**, then `usedown=attackdown=true`, `PST_LIVE`, `health=MAXHEALTH(100)`, `readyweapon=pendingweapon=wp_pistol`, `weaponowned[fist,pistol]=true`, `ammo[clip]=50`, `maxammo=maxammo[]`. The M7 in-place deviation (D017, game.ts:242-249) retires by routing the reborn pass through the same `ga_loadlevel` plumbing M9 builds for exits — `playerstate=PST_DEAD→PST_REBORN` at next load (g_game.c:479-483) is part of the same loop. Tests that flip: e2e death test's world-persistence pin (§3 + JOURNAL:221), `m7exit` guard, and any motion golden whose scripted run crosses a death (re-bless once, reason "D017 retirement — faithful level restart"; grep-verify which `tests/sim/*golden*` files contain a scripted death before wave 2).

0.6. **`d_main.c` frame/tic machine + attract.** `D_DoomLoop` (:354): per iteration `I_StartFrame → (singletics ? 1-tic block : TryRunTics()) → S_UpdateSounds → D_Display` (sound calls = M10). **Tic sourcing** lives in `TryRunTics` (d_net.c:636): `entertic = I_GetTime()/ticdup`, realtics vs availabletics, `counts` clamped ≥1 — our analogue is ALREADY the A-08 rAF accumulator (src/main.ts:183-194, 35 Hz, MAX 4 catch-up tics; documented M5 mapping) — M9 adds only: `advancedemo`/`M_Ticker` run in the **tic block** (d_main.c:381-383), not the display block, and `D_Display` runs once per rAF frame (our renderer call). `D_ProcessEvents` (:161): event ring drained **`M_Responder` FIRST then `G_Responder`** — the menu swallows keys before the game; our eventQueue (main.ts:37/208) feeds the same chain order today for automap keys. `D_Display` (:193-330) draw order per frame: `setsizeneeded⇒R_ExecuteSetViewSize` · state-change ⇒ `wipe_StartScreen` capture · per-state drawers (`GS_LEVEL: AM_Drawer? + ST_Drawer(viewheight==200, redrawsbar)` · `GS_INTERMISSION: WI_Drawer` · `GS_FINALE: F_Drawer` · `GS_DEMOSCREEN: D_PageDrawer` = `V_DrawPatch(0,0,0,pagename)` :434-438) · `R_RenderPlayerView` · `HU_Drawer` · `paused⇒M_PAUSE patch` (:310-316) · **`M_Drawer` last ("menu is drawn even on top of everything", :327 + again inside the wipe loop :344)** · `wipe_EndScreen + wipe_ScreenWipe(wipe_Melt,…)` (f_wipe.c:237/:262; melt = column-per-column drop driven by i_time diffs) whenever `gamestate != wipegamestate`. **Attract** (`GS_DEMOSCREEN`): `D_PageTicker` (:423) decrements `pagetic`, `D_DoAdvanceDemo` (:456) cycles `demosequence%6` (non-retail): TITLEPIC (`pagetic=170`, mus_intro) → `demo1` → CREDIT (200) → demo2 → HELP2/TITLEPIC → demo3 — **demo playback is Post-M12 stretch per ROADMAP §"Post-M12" (".lmp demo playback"); M9 ships the REDUCED attract: TITLEPIC page + any-key→menu via `G_Responder` demo branch (g_game.c:529-541), no demo1/2/3, and `D_StartTitle` (:525, `demosequence=-1`) as the "back to title / end game" target.**

0.7. **`m_menu.c` — menu model, tables, keys, sfx.** Structures: `menuitem_t {status(0 none/1 item/2 thermo), name[10], routine(choice), alphaKey}` + `menu_t {numitems, prevMenu, menuitems, draw-routine, x, y, lastOn}` (:138-169); globals `currentMenu/itemOn/whichSkull/skullAnimCounter`, skull lumps `M_SKULL1/2` (:175), `SKULLXOFF=-32, LINEHEIGHT=16` (:132-133). **No push/backup stack (corrects the brief, §0.1): navigation = `M_SetupNextMenu(&XDef)` (:1824, sets currentMenu+itemOn=lastOn) and back = `currentMenu=prevMenu` (:1681-1689).** Tables: `MainMenu` 6 items `{M_NGAME n, M_OPTION o, M_LOADG l, M_SAVEG s, M_RDTHIS r, M_QUITG q}` (:250-257) — load/save = M11 (register entries, no-op bodies, §4); `EpiDef` (x,y=48,63) 4 items `M_EPI1..4` (:284) shareware-censored `EpiDef.numitems--` in `M_Init` (:1866-1874) — **and `M_Episode` blocks choice≠0 in shareware with an ad message (:919-944)**; `NewDef` (48,63) 5 skills `M_JKILL/M_ROUGH/M_HURT/M_ULTRA/M_NMARE` → `M_ChooseSkill` ⇒ `G_DeferedInitNew(skill, epi+1, 1)` (:898-915) — **skill menu always starts at map 1** — nightmare first arms `M_StartMessage(NIGHTMARE, M_VerifyNightmare, input=true)`; `OptionsDef` (60,37) {endgame, messages, detail, scrnsize(thermo), -, mousesens(thermo), -, soundvol} (:352-363); `SoundDef` 2 thermos (16 dots, `M_THERM*` lumps, vols 0-15); `ReadDef1/2` = full-screen HELP1/HELP2 patches (shareware page 2 = HELP2 :776-789) with `inhelpscreens=true`. **`M_Responder` (:1349) key map**: F1 help (opens panel + jumps ReadDef), F2/F3 save/load (M11), F4 sound, F5 detail, F6/F9 quicksave/load (M11), F7 endgame, F8 toggle messages, F10 quit, F11 gamma (re-sets PLAYPAL palette), F12 spy (in G_Responder), `-`/`=` screen size (blocked when automap/chat :1526-1540), **Esc opens the popup when inactive (:1597-1603)**, within menu: ↑/↓ cycle with `status==-1` skip + `sfx_pstop`, ←/→ thermo (status==2) `sfx_stnmov`, Enter fire (thermo⇒right `sfx_stnmov`, item⇒`sfx_pistol`), Esc⇒`M_ClearMenus` `sfx_swtchx`, Backspace⇒prevMenu `sfx_swtchn`, default = alphaKey scan forward-then-wrapped `sfx_pstop` (:1622-1689). Messages needing input (`messageNeedsInput`) accept only `' ' 'n' 'y' ESC` (:1501-1505). **Mouse: m_menu.c contains ZERO `ev_mouse` handling — vanilla gets menu mouse via the OS layer synthesizing arrow keys; our port MUST synthesize ↑/↓/Enter from hover/wheel/click in the input layer (decision D-0xx §3).** `M_Ticker` (:1834): skull alternates every 8 tics (`skullAnimCounter=10` init). `M_Drawer` (:1740): message-to-print path centered at `y=100-stringheight/2` via `M_WriteText`/hu_font, else draw routine + item patch names at `x,y+i*16` + skull at `x-32, menuY-5+itemOn*16` — **menus draw DIRECT to screen (V_DrawPatchDirect), not through the status-bar BG layer**. Quit text: `M_QuitDOOM` random-picks `endstring` from quitsounds/quitsounds2 gated `(gametic>>2)&7` (:1066-1091) — **`gametic`-derived, deterministic per tic**.

0.8. **`st_stuff.c` statusbar + `st_lib.c` widgets + messages-in-HU.** Layout/consts (st_stuff.c:78-250): bar 320×32 at y=168 (`ST_X=0, ST_X2=104, ST_FX=143, ST_FY=169`; ST_Y=SCREENHEIGHT-32, st_stuff.h:32-34); AMMO num (44,171) w3, HEALTH percent (90,171), ARMS bg (104,168) + 6 weapon nums `ST_ARMSX=111+12*(i%3), ST_ARMSY=172+10*(i/3)`, KEYS **x=239, y=171/181/191** (8×5), armor percent (221,171), ammo tall-nums 288 y=173/179/191/185, max-ammo 314 same ys, frags (138,171) DM-only. **Lumps (ST_loadGraphics :1124-1197): `STTNUM0-9` (tall, ammo/health/armor/frags), `STYSNUM0-9` (short), `STTPRCNT`, `STKEYS0-5` (NUMCARDS — one patch per color, dim-vs-lit choice is `keyboxes[i] = cards[i]?i : cards[i+3]?i+3 : -1` :949-954 — **the "KRE/Y" guess in the brief is wrong: one lump per index 0..5, blue/yellow/red × off/on**), `STARMS` bg, arms gray nums `STGNUM2-7` (+yellow = STYSNUM reuse :1146-1153), face bg `STFB0-3` (per-player, netgame-only draw :508-509), `STBAR` bg, faces `STFST<pain><0-2>`, `STFTR<p>0`, `STFTL<p>0`, `STFOUCH<p>`, `STFEVL<p>`, `STFKILL<p>` (p=0..4 pain tier) + `STFGOD0` + `STFDEAD0` = 42 (`ST_FACESTRIDE=8, ST_NUMPAINFACES=5` :89-105); minus `STTMINUS` (st_lib.c).** **Face machine `ST_updateFaceWidget` (:752), precedence `dead > evil grin > attacked > self-hurt > rampage > invul > idle`** with `priority` latch: dead (`!health`) `ST_DEADFACE(=ST_GODFACE+1=41)` 1 tic; evil grin on **weapon-owned delta + bonuscount** `priority=8`, 70 tics (`ST_EVILGRINCOUNT=2*TICRATE`), index `painOffset+ST_EVILGRINOFFSET(=5)`; attacked (`damagecount && attacker && attacker!=mo`) — `health-oldhealth > ST_MUCHPAIN(20)` ⇒ OUCH (`ST_OUCHOFFSET=4`, 35 tics) **else** direction: `R_PointToAngle2(mo→attacker)` vs view angle, `diffang<ANG45`⇒RAMPAGE(6) else turn R/L (`ST_TURNOFFSET=3` +i? the confusing `i` branch :818-836) 35 tics; self-hurt (`damagecount` alone, attacker==mo) OUCH-or-RAMPAGE (priority 7/6); **rampage: `attackdown` held `ST_RAMPAGEDELAY=70` consecutive tics ⇒ pissed face 1 tic**; god/invul `ST_GODFACE(=40)`; idle fallback `painOffset + (M_Random() % 3)` every `ST_STRAIGHTFACECOUNT=17` tics — **1 `M_Random()` draw per statusbar tic** (`ST_Ticker` :988: `st_randomnumber = M_Random()` UNCONDITIONALLY every GS_LEVEL tic — a menu-stream draw M9 must ledger, and it changes every downstream WI animation draw §0.9!). `ST_calcPainOffset` (:733): `ST_FACESTRIDE * ((100-min(health,100))*5/101)` — 5 pain tiers (0=healthy … 4=near-dead). Key/arms/frags/mode gates: `st_armson = statusbaron && !deathmatch`, `st_fragson = deathmatch && statusbaron` (:924-971). **Widget mechanics (st_lib.c): numbers right-justified from (x,y) drawing `width` digits LSB-first, `if(!num) draw '0'`, 1994 = "n/a" skip (:136-146 — `w_ready.num = &largeammo(1994)` for no-ammo weapons st_stuff.c:928-932), minus at x-8; per-digit `V_DrawPatch` of `n->p[digit]`; percent draws `STTPRCNT` at the right edge first (refresh-only); multicon (`w_faces`, `w_arms`, `w_keyboxes`) draws `p[*inum]` and erases via `V_CopyRect(BG→FG)` of the OLD patch's box; binicon (armsbg) draws-or-erases on value flip.** The bar is composited in an **off-screen BG canvas** (`screens[4]`, ST_Init :1470): `ST_refreshBackground` draws `STBAR`(+faceback) into BG then `V_CopyRect` BG→FG (:499-514); `ST_Drawer(fullscreen, refresh)` (:1108): `st_statusbaron = !fullscreen || automapactive` (fullscreen ⇔ `viewheight==200` from D_Display :252), `st_firsttime/refresh ⇒ full redraw else diff` (incremental widget-only). `ST_Responder` (:518) = the CHEAT layer — cheat sequences defined at st_stuff.c:400-470 (`iddqd` god, `idfa`/`idkfa`, `idmus<nn>`, `idsp*` choppers (commercial-gated), `iddt`, `idbehold*` powerup family (`cheat_powerup_seq[7]` :441, **`idclev<ep><map>` ⇒ `G_DeferedInitNew(gameskill, epsd, map)` :700-724 — a game-flow seam M9 must satisfy for the cheat to work**, `mypos`) + automap state tracking — message display is NOT here. **Messages live in `hu_stuff.c`: `HU_Ticker` (:505) pops `plr->message` into 1-line widget `w_message` (HU_MSGX/Y=0,0, `HU_MSGTIMEOUT=4*TICRATE=140` hu_stuff.h:44, `showMessages` cvar gate, `message_dontfuckwithme` for `HUSTR_FOUNDSECRET`-style priority lines :518-565); font = `STCFN%.3d` lumps `'!'`(33)…`'_'`(95), `HU_FONTSIZE=63` (hu_stuff.h:31-34, hu_stuff.c:392-414); `HU_Drawer` draws message + chat, map-title only over automap.** The brief's "ST_message/ST_saytime/LIFETIME" = these HU pieces (old-source names, §0.1); **no tips lumps exist anywhere in 1.10 (grep §0.1) — "tips" is a DOOM-2-retail-only feature, out of scope.**

0.9. **`wi_stuff.c` intermission (the real "level transition screen").** `WI_Start` (:1838) ← `G_DoCompleted`; `WI_initVariables` (:1796) clamps `maxkills/items/secret` to ≥1 (**divide-by-zero guard = the 100% denominator**) and `gamestate!=retail && epsd>2 ⇒ epsd-=3`. SP state machine `sp_state` (init :1318, update :1330): **1 pause(35) → 2 kills count-up (`cnt_kills += 2`/tic, `sfx_pistol` every 4th tic `!(bcnt&3)`, stop+`sfx_barexp` at `(skills*100)/maxkills`) → 3 pause → 4 items → 5 pause → 6 secrets → 7 pause → 8 TIME counts `+=3` toward `stime/35` AND par `+=3` toward `partime/35` (both clamp; done when both land) → 10 wait-for-accelerate ⇒ `WI_initShowNextLoc`** (or `WI_initNoState` commercial). Percent math is INTEGER DIVISION of the live counters; `cnt_pause = TICRATE` between stages. `WI_checkForAccelerate` (:1470): attack/use **edge** ⇒ `acceleratestage` (also the fast-forward: state≠10 jumps all counters to final + one `sfx_barexp`, :1338-1351). `ShowNextLoc` (:752-812): `cnt = SHOWNEXTLOCDELAY(4)*35` tics (:299), pointer blink `snl_pointeron = (cnt&31) < 20`, splats on maps `0..last` (+8 if didsecret) via `WI_drawOnLnode` (`lnodes[epsd][n]`, WISPLAT + WIURH0/1 flip), `WI_drawEL` "Entering <name>"; `NoState` (:730): `cnt=10` then **`G_WorldDone()`** — the whole next-level handoff. Music: `bcnt==1 ⇒ S_ChangeMusic(mus_inter)` (:1509-1514). Draw: `WI_drawStats` (:1436) slams screen1→0 background (`WI_slamBackground` :406, `bg` = `WIMAP<epsd>` patch at 0,0 :1538-1557), `WI_drawLF` (:421) `<WILV epsd,last>` + "Finished!" (WIF) at y=2 + 5/4-height spacing, labels at SP_STATSX/Y=50/50 (`WIOSTK`/`WIOSTI`/`WISCRT2`) with right-side `WI_drawPercent` = WIPCNT + `WINUM0-9` right-justified (`WI_drawNum` :669, 1994="n/a" skip, WIMINUS), TIME row at (16, 168) `WITIME` + `WI_drawTime` base-60 split (:683-717, `sucks` WISUCKS over 61*59), par `WIPAR` right half if epsd<3. **PRNG coupling: `WI_initAnimatedBack` (:503) draws `M_Random()%period` per ANIM_ALWAYS/ANIM_RANDOM anim at each state entry, AND `WI_updateAnimatedBack` (:532) draws `M_Random()%data1` again on every ANIM_RANDOM *loop* (wi_stuff.c:557-564) — episode-1 has NUMANIMS[0] animations; these share the MENU-stream `M_Random` with the statusbar face RNG (§0.8) — the ledger must count `ST_Ticker`'s draw while a level is live, or the animated intermission desyncs.** Sfx sites use `S_StartSound(0,…)` (world-less); silent-M9 keeps them as counters (§3).

0.10. **`f_finale.c` endgame.** `F_StartFinale` (:96): `gamestate=GS_FINALE`, `mus_victor`, **episodic ep1: `finaleflat="FLOOR4_8"`, `finaletext=E1TEXT`** — the text is a **compiled-in C string (d_englsh.h:359), there is NO F1TEXT endgame LUMP** (brief's "F1TEXT?" → measured absent from both source and WAD, §0.12; "lumps present-in-freedoom audit" = moot, the flat FLOOR4_8 + font + HELP2 are what must exist, all measured present). `F_Ticker` (:207): text phase until `finalecount > strlen(text)*TEXTSPEED(3) + TEXTWAIT(250)` ⇒ `finalestage=1` + `wipegamestate=-1`; commercial-only skip-after-50 + `gamemap==30 ⇒ F_StartCast` (bunny/cast = Doom-2/E3 scope, §4). `F_TextWrite` (:261): 64×64 flat tile flood of the WHOLE screen, then 1 char/tic-ish (`count=(finalecount-10)/3`) hu_font uppercase glyphs at (10,10), +11 y per \n. Stage 1 draw (`F_Drawer` :700): **shareware/registered ep1 ⇒ `HELP2` patch forever — no timeout, no key-exit: the ONLY way onward is opening the control panel (F-key/Esc via M_Responder, which runs in every state), or M_EndGame→`D_StartTitle`** — measured truth for "end screen → title" flow; `F_Responder` only eats events in stage 2. E1M8 exit is `ga_victory` from `G_DoCompleted` (§0.3) — never through `G_WorldDone`.

0.11. **Renderer additions M9 needs: `V_DrawPatch`, BG layer, view size.** `V_DrawPatch` (v_video.c:204-263): `x -= leftoffset; y -= topoffset`, per-column walk of `patch_t` (`{width,height,leftoffset,topoffset,columnofs[width]}`, r_defs.h:356-364; columns = `post_t {topdelta,length}` chains, 0xff-terminated, r_defs.h:285-292) copying raw palette bytes — **NO translation table and NO colormap parameter in this path; `CRANG`/TRANMAP do not exist in 1.10 (§0.1). Palette-level effects are done by `I_SetPalette` (gamma `usegamma` F11 + `ST_doPaletteStuff` damage/bonus/radiation palettes 1-8/9-12/13 st_stuff.c:69-74,1000), and view-only shading by the COLORMAP lump (r_data.c:639) — patches for menu/HUD/intermission are always full-bright raw indices.** Screens: `screens[0]`=FG, `screens[4]`=statusbar BG canvas (`#define BG 4` st_lib.h:33; 320×32 alloc ST_Init :1470); `V_CopyRect(BG→FG)` = the widget erase primitive. **View/statusbar interplay (the M4-renderer param FINDING): our port hard-codes fullscreen 320×200 (src/render/view.ts:60-66, "viewheight=SCREENHEIGHT"), and vanilla only shrinks the view via `R_ExecuteSetViewSize` (r_main.c:671-690): screenblocks 11 ⇒ `scaledviewwidth=320, viewheight=200` (no bar); 1-10 ⇒ `scaledviewwidth=setblocks*32, viewheight=(setblocks*168/10)&~7` (r_main.c:689-690; 10 ⇒ 168 — the brief's "200→112" is wrong at default; 112 corresponds to no vanilla block), `centery=viewheight/2`, `yslope` rebuilt (:730-734), `pspritescale=FRACUNIT*viewwidth/320` (:722); `R_InitBuffer` (r_draw.c:696-723): `viewwindowx=(320-width)>>1`, `viewwindowy = width==320 ? 0 : (200-32-height)>>1` — **at the DEFAULT screenblocks=10 the 320-wide view is TOP-aligned (viewwindowy=0) over a 168-high window and the bar simply owns rows 168-199**; the bar itself is never a viewport cut — `ST_Drawer(viewheight==200, …)` (d_main.c:252) just tells ST whether to paint itself. `R_FillBackScreen` (r_draw.c:731: GRNDSWICH/FLOOR4_8-pattern back fill + M_DOOM-less border) and `R_DrawViewBorder` (:832) only matter for windowed sizes.** `f_wipe.c:237/:262` melt for state changes (`wipegamestate` sentinel, d_main.c:216-222/:332-346) — runs in DISPLAY time (I_GetTime), NOT tics: intentionally non-deterministic, plan mirrors with rAF-time animation excluded from sim hashes.

0.12. **Freedoom specifics — measured on the pinned `wads/freedoom1.wad` this pass** (parsed with the repo's own `src/wad/wadfile.ts`, 3163 lumps; a first raw-node parse attempt mis-read the directory layout — corrected by using the tested loader): **THE WAD CONTAINS ALL 36 MAPS E1M1..E4M9** (Freedoom ships Phase 1/2/3 in one doom1-style IWAD) — so "Freedoom = single episode Phase 1" is a **policy choice for this port, not a data constraint**: the lever is the `gamemode` constant + `G_InitNew`'s shareware clamp to episode 1 (§0.4); with `retail`-like semantics the same code reaches E2+ (M12 corpus). E1 = maps 1-9 incl. E1M9 (the "Home" hub: E1M8 exit ⇒ finale, E1M9 exit ⇒ E1M4, §0.3). **Lump audit: ALL 1.10-required M9 graphics are PRESENT** — STTNUM/STYSNUM/STGNUM/STTPRCNT/STTMINUS, STKEYS0-5, STARMS, STFB0-3, STBAR, all 42 faces (46 `STF*` = 42 + STFB0-3), M_NGAME/M_OPTION/M_LOADG/M_SAVEG/M_RDTHIS/M_QUITG, M_EPISOD + M_EPI1-4, M_NEWG/M_SKILL/M_JKILL/M_ROUGH/M_HURT/M_ULTRA/M_NMARE, M_THERMO/M_THERM{L,R,M}, M_SVOL/M_SFXVOL/M_MUSVOL, M_PAUSE, M_DOOM, M_SKULL1/2; intermission WIMAP0 (+1,2), WILV00-08, WIA anims (58), WIURH0/1, WISPLAT, WIPCNT/WIF/WIENTER/WIOSTK/WIOSTI/WISCRT2/WICOLON/WITIME/WISUCKS/WIPAR/WIMINUS/WINUM0-9; TITLEPIC/CREDIT/HELP1/HELP2/ENDPIC/VICTORY2 present, FLOOR4_8 present; STCFN 33-96 + a few extras (all of `!`-`_` renderable, 68 lumps). **GOTCHA: the WAD also carries a Crispy-style SUPERSET menu set (M_VIDEO/M_WAD/M_CRISPY/M_KEYBND/…, 82 `M_*` lumps) — M9 must render the VANILLA menu graph, ignore the extras; also `M_EPI2-4` exist so the shareware-censoring (§0.7 M_Init) is code-side, and `D_READ_M`/`D_DM2TTL` (Doom-2 tracks) are ABSENT while `D_VICTOR` (finale) is present (music = M10).** No `CWILV*`/`INTERPIC` (correct for non-commercial). `F1TEXT` absent (never existed, §0.10).

0.13. **TS seams as-is (audited this pass).** `src/sim/game.ts`: `gTicker` reborn pass = D017 in-place deviation (:242-255), step 2 gameaction drain EMPTY (:251) — M9 turns both on; `src/main.ts`: 35 Hz accumulator (:183-194) + eventQueue + `attachPopInput`; `src/input/keyboard.ts`: `DEFAULT_EVENT_CODES` covers automap keys ONLY (:76-89) — menu keys (Esc/arrows/Enter/F1-F12/alphas) and the held-vs-event split audit are the keyboard-layer task; `src/render/rthings.ts:232/491` still excludes `KIND_MONSTER` (the ROADMAP M9-preview carry-over "production monster pixels"); `src/render/view.ts` fullscreen-only params (§0.11); `src/debug.ts` seams (`debugSim`, mouse injection :86) grow menu/HUD reads; e2e specs live in `e2e/*.spec.ts` with the `state()`/warp harness — zero-console-errors is already an assertion idiom.

## 1. Task graph and parallel waves

(filling)

## 2. Per-task specifications

(filling)

## 3. Deviations, deferred hooks, and open decisions

(filling)

## 4. Exit criteria (milestone-level, objectively checkable)

(filling)

## 5. Final report

(filling)
