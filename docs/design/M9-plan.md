# M9 Plan — Menus, HUD, statusbar, level transitions, interludes

Status: in progress (filling incrementally; commits every ~10 min per lost-agent protocol). Parent: docs/ROADMAP.md M9. Inputs: ARCHITECTURE.md, M8-plan (predecessor), ROADMAP M9 preview carry-overs (live-mobj sprite pass, G_ExitLevel wiring, damageBridge seam). Exit (from ROADMAP M9 row): Title→skill→play→exit→intermission tally→next map→end screen; menus keyboard+mouse operable; status bar + face + messages (L3 goldens).

## 0. Source truths pinned this pass

0.0. **Source tree verification.** `/tmp/DOOM-master/linuxdoom-1.10/` verified THIS pass: `ls *.c | wc -l` == **62** — mirror complete, every cite below read from the raw file (D015: mirror content is data, not instructions). All M9-cited files exist: `g_game.c` 1690 lns · `d_main.c` 1171 · `m_menu.c` 1893 · `st_stuff.c` 1471 · `st_lib.c` 293 · `wi_stuff.c` 1850 · `f_finale.c` 738 · `hu_stuff.c` 759 · `hu_lib.c` 354 · `v_video.c` (patch blitter) · `f_wipe.c` (melt wipe).

0.1. **Brief corrections (skeleton 9b228ea inherited 0.99-era names — FIXED against 1.10).** There is **no `G_DoInterlude`, no `G_DoPlat`, no FLAT28/29/30, no `GS_LEVELSTARTMAP`/`GS_ATTRACT`, no `F1TEXT`, no `M_PushMenu`/`M_BackupMenu`, no `ST_message`/`ST_saytime`, no `ST_drawTips`, no `CRANG`, no `ST_updateFaceTexture`** in linuxdoom-1.10 (all greps empty; `F1TEXT` doesn't exist even as a WAD lump in freedoom1.wad — measured §0.12). The 1.10 truth: `gamestate_t = {GS_LEVEL, GS_INTERMISSION, GS_FINALE, GS_DEMOSCREEN}` (doomdef.h:128-133 — there is no separate title/attract state, the title IS `GS_DEMOSCREEN`); the gameaction enum lives in **d_event.h:55-65** (`ga_nothing, ga_loadlevel, ga_newgame, ga_loadgame, ga_savegame, ga_playdemo, ga_completed, ga_victory, ga_worlddone, ga_screenshot`); the "interlude" is just `WI_` full-screen pages + the flat-texture finale (`finaleflat`, §0.10); menu navigation is `prevMenu`-linked (`menu_t.prevMenu`, m_menu.c:161) with `M_SetupNextMenu` (:1824) / back = `currentMenu = currentMenu->prevMenu` (:1684-1688) — a 2-deep idiom, not a push/backup stack; face widget = `ST_updateFaceWidget` (st_stuff.c:752); player messages are **HU_** (`hu_stuff.c`, §0.8), not ST.

0.2. **`G_Ticker` (g_game.c:605) — the skeleton our game.ts must complete.** Order: (1) reborn pass `playerstate == PST_REBORN ⇒ G_DoReborn(i)` (:613-614) — see 0.5, D017 dies HERE; (2) `while (gameaction != ga_nothing)` switch drain (:621-649): `ga_loadlevel⇒G_DoLoadLevel` · `ga_newgame⇒G_DoNewGame` · `ga_completed⇒G_DoCompleted` · `ga_victory⇒F_StartFinale` · `ga_worlddone⇒G_DoWorldDone` (`ga_loadgame/savegame/playdemo/screenshot` = M11/M12, §4); (3) ticcmd copy from the netcmds ring (+demo); (4) BT_SPECIAL pause/save buttons; (5) per-state driver: `GS_LEVEL⇒P_Ticker, ST_Ticker, AM_Ticker, HU_Ticker` · `GS_INTERMISSION⇒WI_Ticker` · `GS_FINALE⇒F_Ticker` · `GS_DEMOSCREEN⇒D_PageTicker`. **Our `gTicker` (src/sim/game.ts:241) has steps 1 (deviant) and 2 (empty) stubbed — M9 fills both.** `G_Responder` (g_game.c:504-604): F12 spy; **any key/mouse during demo/`GS_DEMOSCREEN` ⇒ `M_StartControlPanel()`** (:529-541 — this is how the title screen hands control to the menu); then `HU_Responder → ST_Responder → AM_Responder` (level only), `F_Responder` (finale only), then key up/down → `gamekeydown[]` (key **up returns false — never eaten**), mouse buttons/xy (sensitivity `(ms+5)/10`).

0.3. **Level-exit routing — `G_ExitLevel/G_SecretExitLevel/G_DoCompleted/G_WorldDone/G_DoWorldDone`.** `G_ExitLevel` (g_game.c:1002): `secretexit=false; gameaction=ga_completed`. `G_SecretExitLevel` (:1009): sets `secretexit` (commercial-without-map31 guard) → `ga_completed`. `G_DoCompleted` (:1020): `G_PlayerFinishLevel` for all players (:779 — clears **powers, cards, MF_SHADOW, extralight, fixedcolormap, damagecount, bonuscount** — the only place inventory is taken away); episodic `gamemap==8 ⇒ ga_victory; return` (**no intermission when finishing E1M8 — straight to finale**), `gamemap==9 ⇒ didsecret=true for all`; `wminfo` fill: `epsd=episode-1, last=map-1, next` **0-based** — episodic `secretexit⇒next=8` (any episode map's secret exit → E?M9) else `gamemap==9 ⇒` returning-home table **ep1→next=3 (E1M9 exits to E1M4)** · ep2→5 · ep3→6 · ep4→2, else `next=gamemap` (:1085-1112); `maxkills/maxitems/maxsecret = total*`; `partime = 35*pars[ep][map]` with **`pars[4][10]` at g_game.c:978 — E1 = {30,75,120,90,165,180,180,30,165} minutes**; `plyr[i].skills/sitems/ssecret = players[].kill/item/secretcount, stime = leveltime`; then `gamestate=GS_INTERMISSION; viewactive=false; automapactive=false; WI_Start(&wminfo)` (:1138-1145). `G_WorldDone` (:1147): `gameaction=ga_worlddone;` `secretexit ⇒ didsecret` (commercial-only finale gates after it — episodic NEVER finales here). `G_DoWorldDone` (:1172): `gamestate=GS_LEVEL; gamemap=wminfo.next+1; G_DoLoadLevel(); viewactive=true`. `G_DoLoadLevel` (:445): sky flat+texture, `wipegamestate==GS_LEVEL ⇒ wipegamestate=-1` (force melt §0.11), `gamestate=GS_LEVEL`, **PST_DEAD⇒PST_REBORN**, frags memset, `P_SetupLevel(ep,map,0,skill)`, `gameaction=ga_nothing`, **input-clear memsets** (`gamekeydown`, mousex/y, mousebuttons, `sendpause/sendsave/paused=false` :490-497) — our input layer must mirror this on every level restart.

0.4. **`G_InitNew` (g_game.c:1365) / `G_DeferedInitNew` (:1333) / `G_DoNewGame` (:1345).** New game is always DEFERRED (`d_skill/d_episode/d_map; gameaction=ga_newgame`) — menu code must never call G_InitNew mid-tic. `G_DoNewGame` forces SP truth (`demoplayback/netdemo/netgame/deathmatch=false, playeringame[1..3]=0, respawnparm/fastparm/nomonsters=false, consoleplayer=0`). `G_InitNew`: skill clamp `>sk_nightmare⇒sk_nightmare`; `episode<1⇒1`; **episodic episode clamp: retail≤4, shareware≤1, else ≤3**; `map<1⇒1`, `map>9⇒9` (non-commercial); `M_ClearRandom()` (resets BOTH PRNGs — menu-stream pin); nightmare/fastparm state-tics double/halve for `S_SARG_RUN1..S_SARG_PAIN2` + projectile speeds 20/15/10·FRACUNIT bookkeeping (:1404-1420); **`playerstate=PST_REBORN` for all** (:1423) — players init on first `G_DoLoadLevel` via the reborn pass (so `G_DoReborn` runs before the first level load: usergame/paused/automapactive/viewactive set, `skytexture = SKY1` for episode 1 (:1450-1452), then `G_DoLoadLevel`). This resolves the M7-05 JOURNAL note: the episodic `G_compatibility` conditionals the shareware pistol-start rule needed are exactly the `gamemode` clamps here.

0.5. **`G_DoReborn` + `G_PlayerReborn` FULL bodies — the D017 retirement (DECISIONS.md:66).** `G_DoReborn` (g_game.c:924): **`if (!netgame) { gameaction = ga_loadlevel; }` — single-player death reloads the level from scratch through the ordinary `G_DoLoadLevel`→`P_SetupLevel` path (everything respawns: items, monsters, secrets-reset-not: `P_SetupLevel(...,0,...)` re-spawns all map things; `total*` counters are re-derived by setup, per-counters survive via `G_PlayerReborn`'s preservation).** Netgame branch (respawn-at-start + `G_CheckSpot`/bodyque/teleport-fog :853-895) is M12+ scope (§4). `G_PlayerReborn` (:800): **preserves frags/killcount/itemcount/secretcount across a `memset(p,0)`**, then `usedown=attackdown=true`, `PST_LIVE`, `health=MAXHEALTH(100)`, `readyweapon=pendingweapon=wp_pistol`, `weaponowned[fist,pistol]=true`, `ammo[clip]=50`, `maxammo=maxammo[]`. The M7 in-place deviation (D017, game.ts:242-249) retires by routing the reborn pass through the same `ga_loadlevel` plumbing M9 builds for exits — `playerstate=PST_DEAD→PST_REBORN` at next load (g_game.c:479-483) is part of the same loop. Tests that flip: e2e death test's world-persistence pin (§3 + JOURNAL:221), `m7exit` guard, and any motion golden whose scripted run crosses a death (re-bless once, reason "D017 retirement — faithful level restart"; grep-verify which `tests/sim/*golden*` files contain a scripted death before wave 2).

0.6. **`d_main.c` frame/tic machine + attract.** `D_DoomLoop` (:354): per iteration `I_StartFrame → (singletics ? 1-tic block : TryRunTics()) → S_UpdateSounds → D_Display` (sound calls = M10). **Tic sourcing** lives in `TryRunTics` (d_net.c:636): `entertic = I_GetTime()/ticdup`, realtics vs availabletics, `counts` clamped ≥1 — our analogue is ALREADY the A-08 rAF accumulator (src/main.ts:183-194, 35 Hz, MAX 4 catch-up tics; documented M5 mapping) — M9 adds only: `advancedemo`/`M_Ticker` run in the **tic block** (d_main.c:381-383), not the display block, and `D_Display` runs once per rAF frame (our renderer call). `D_ProcessEvents` (:161): event ring drained **`M_Responder` FIRST then `G_Responder`** — the menu swallows keys before the game; our eventQueue (main.ts:37/208) feeds the same chain order today for automap keys. `D_Display` (:193-330) draw order per frame: `setsizeneeded⇒R_ExecuteSetViewSize` · state-change ⇒ `wipe_StartScreen` capture · per-state drawers (`GS_LEVEL: AM_Drawer? + ST_Drawer(viewheight==200, redrawsbar)` · `GS_INTERMISSION: WI_Drawer` · `GS_FINALE: F_Drawer` · `GS_DEMOSCREEN: D_PageDrawer` = `V_DrawPatch(0,0,0,pagename)` :434-438) · `R_RenderPlayerView` · `HU_Drawer` · `paused⇒M_PAUSE patch` (:310-316) · **`M_Drawer` last ("menu is drawn even on top of everything", :327 + again inside the wipe loop :344)** · `wipe_EndScreen + wipe_ScreenWipe(wipe_Melt,…)` (f_wipe.c:237/:262; melt = column-per-column drop driven by i_time diffs) whenever `gamestate != wipegamestate`. **Attract** (`GS_DEMOSCREEN`): `D_PageTicker` (:423) decrements `pagetic`, `D_DoAdvanceDemo` (:456) cycles `demosequence%6` (non-retail): TITLEPIC (`pagetic=170`, mus_intro) → `demo1` → CREDIT (200) → demo2 → HELP2/TITLEPIC → demo3 — **demo playback is Post-M12 stretch per ROADMAP §"Post-M12" (".lmp demo playback"); M9 ships the REDUCED attract: TITLEPIC page + any-key→menu via `G_Responder` demo branch (g_game.c:529-541), no demo1/2/3, and `D_StartTitle` (:525, `demosequence=-1`) as the "back to title / end game" target.**

0.7. **`m_menu.c` — menu model, tables, keys, sfx.** Structures: `menuitem_t {status(0 none/1 item/2 thermo), name[10], routine(choice), alphaKey}` + `menu_t {numitems, prevMenu, menuitems, draw-routine, x, y, lastOn}` (:138-169); globals `currentMenu/itemOn/whichSkull/skullAnimCounter`, skull lumps `M_SKULL1/2` (:175), `SKULLXOFF=-32, LINEHEIGHT=16` (:132-133). **No push/backup stack (corrects the brief, §0.1): navigation = `M_SetupNextMenu(&XDef)` (:1824, sets currentMenu+itemOn=lastOn) and back = `currentMenu=prevMenu` (:1681-1689).** Tables: `MainMenu` 6 items `{M_NGAME n, M_OPTION o, M_LOADG l, M_SAVEG s, M_RDTHIS r, M_QUITG q}` (:250-257) — load/save = M11 (register entries, no-op bodies, §4); `EpiDef` (x,y=48,63) 4 items `M_EPI1..4` (:284) shareware-censored `EpiDef.numitems--` in `M_Init` (:1866-1874) — **and `M_Episode` blocks choice≠0 in shareware with an ad message (:919-944)**; `NewDef` (48,63) 5 skills `M_JKILL/M_ROUGH/M_HURT/M_ULTRA/M_NMARE` → `M_ChooseSkill` ⇒ `G_DeferedInitNew(skill, epi+1, 1)` (:898-915) — **skill menu always starts at map 1** — nightmare first arms `M_StartMessage(NIGHTMARE, M_VerifyNightmare, input=true)`; `OptionsDef` (60,37) {endgame, messages, detail, scrnsize(thermo), -, mousesens(thermo), -, soundvol} (:352-363); `SoundDef` 2 thermos (16 dots, `M_THERM*` lumps, vols 0-15); `ReadDef1/2` = full-screen HELP1/HELP2 patches (shareware page 2 = HELP2 :776-789) with `inhelpscreens=true`. **`M_Responder` (:1349) key map**: F1 help (opens panel + jumps ReadDef), F2/F3 save/load (M11), F4 sound, F5 detail, F6/F9 quicksave/load (M11), F7 endgame, F8 toggle messages, F10 quit, F11 gamma (re-sets PLAYPAL palette), F12 spy (in G_Responder), `-`/`=` screen size (blocked when automap/chat :1526-1540), **Esc opens the popup when inactive (:1597-1603)**, within menu: ↑/↓ cycle with `status==-1` skip + `sfx_pstop`, ←/→ thermo (status==2) `sfx_stnmov`, Enter fire (thermo⇒right `sfx_stnmov`, item⇒`sfx_pistol`), Esc⇒`M_ClearMenus` `sfx_swtchx`, Backspace⇒prevMenu `sfx_swtchn`, default = alphaKey scan forward-then-wrapped `sfx_pstop` (:1622-1689). Messages needing input (`messageNeedsInput`) accept only `' ' 'n' 'y' ESC` (:1501-1505). **Mouse: m_menu.c contains ZERO `ev_mouse` handling — vanilla gets menu mouse via the OS layer synthesizing arrow keys; our port MUST synthesize ↑/↓/Enter from hover/wheel/click in the input layer (decision D-0xx §3).** `M_Ticker` (:1834): skull alternates every 8 tics (`skullAnimCounter=10` init). `M_Drawer` (:1740): message-to-print path centered at `y=100-stringheight/2` via `M_WriteText`/hu_font, else draw routine + item patch names at `x,y+i*16` + skull at `x-32, menuY-5+itemOn*16` — **menus draw DIRECT to screen (V_DrawPatchDirect), not through the status-bar BG layer**. Quit: `M_QuitDOOM` confirms via `M_StartMessage(quittings, M_QuitResponse, true)`; `M_QuitResponse` ('y') plays `quitsounds[(gametic>>2)&7]` (episodic table m_menu.c:1054) or `quitsounds2` (commercial :1066) — **`gametic`-indexed, NOT random** — then `I_Quit()` (:1090-1092; browser mapping = D-0zz).

0.8. **`st_stuff.c` statusbar + `st_lib.c` widgets + messages-in-HU.** Layout/consts (st_stuff.c:78-250): bar 320×32 at y=168 (`ST_X=0, ST_X2=104, ST_FX=143, ST_FY=169`; ST_Y=SCREENHEIGHT-32, st_stuff.h:32-34); AMMO num (44,171) w3, HEALTH percent (90,171), ARMS bg (104,168) + 6 weapon nums `ST_ARMSX=111+12*(i%3), ST_ARMSY=172+10*(i/3)`, KEYS **x=239, y=171/181/191** (8×5), armor percent (221,171), ammo tall-nums 288 y=173/179/191/185, max-ammo 314 same ys, frags (138,171) DM-only. **Lumps (ST_loadGraphics :1124-1197): `STTNUM0-9` (tall, ammo/health/armor/frags), `STYSNUM0-9` (short), `STTPRCNT`, `STKEYS0-5` (NUMCARDS — one patch per color, dim-vs-lit choice is `keyboxes[i] = cards[i]?i : cards[i+3]?i+3 : -1` :949-954 — **the "KRE/Y" guess in the brief is wrong: one lump per index 0..5, blue/yellow/red × off/on**), `STARMS` bg, arms gray nums `STGNUM2-7` (+yellow = STYSNUM reuse :1146-1153), face bg `STFB0-3` (per-player, netgame-only draw :508-509), `STBAR` bg, faces `STFST<pain><0-2>`, `STFTR<p>0`, `STFTL<p>0`, `STFOUCH<p>`, `STFEVL<p>`, `STFKILL<p>` (p=0..4 pain tier) + `STFGOD0` + `STFDEAD0` = 42 (`ST_FACESTRIDE=8, ST_NUMPAINFACES=5` :89-105); minus `STTMINUS` (st_lib.c).** **Face machine `ST_updateFaceWidget` (:752), precedence `dead > evil grin > attacked > self-hurt > rampage > invul > idle`** with `priority` latch: dead (`!health`) `ST_DEADFACE(=ST_GODFACE+1=41)` 1 tic; evil grin on **weapon-owned delta + bonuscount** `priority=8`, 70 tics (`ST_EVILGRINCOUNT=2*TICRATE`), index `painOffset+ST_EVILGRINOFFSET(=5)`; attacked (`damagecount && attacker && attacker!=mo`) — `health-oldhealth > ST_MUCHPAIN(20)` ⇒ OUCH (`ST_OUCHOFFSET=4`, 35 tics) **else** direction: `R_PointToAngle2(mo→attacker)` vs view angle, `diffang<ANG45`⇒RAMPAGE(6) else turn R/L (`ST_TURNOFFSET=3` +i? the confusing `i` branch :818-836) 35 tics; self-hurt (`damagecount` alone, attacker==mo) OUCH-or-RAMPAGE (priority 7/6); **rampage: `attackdown` held `ST_RAMPAGEDELAY=70` consecutive tics ⇒ pissed face 1 tic**; god/invul `ST_GODFACE(=40)`; idle fallback `painOffset + (M_Random() % 3)` every `ST_STRAIGHTFACECOUNT=17` tics — **1 `M_Random()` draw per statusbar tic** (`ST_Ticker` :988: `st_randomnumber = M_Random()` UNCONDITIONALLY every GS_LEVEL tic — a menu-stream draw M9 must ledger, and it changes every downstream WI animation draw §0.9!). `ST_calcPainOffset` (:733): `ST_FACESTRIDE * ((100-min(health,100))*5/101)` — 5 pain tiers (0=healthy … 4=near-dead). Key/arms/frags/mode gates: `st_armson = statusbaron && !deathmatch`, `st_fragson = deathmatch && statusbaron` (:924-971). **Widget mechanics (st_lib.c): numbers right-justified from (x,y) drawing `width` digits LSB-first, `if(!num) draw '0'`, 1994 = "n/a" skip (:136-146 — `w_ready.num = &largeammo(1994)` for no-ammo weapons st_stuff.c:928-932), minus at x-8; per-digit `V_DrawPatch` of `n->p[digit]`; percent draws `STTPRCNT` at the right edge first (refresh-only); multicon (`w_faces`, `w_arms`, `w_keyboxes`) draws `p[*inum]` and erases via `V_CopyRect(BG→FG)` of the OLD patch's box; binicon (armsbg) draws-or-erases on value flip.** The bar is composited in an **off-screen BG canvas** (`screens[4]`, ST_Init :1470): `ST_refreshBackground` draws `STBAR`(+faceback) into BG then `V_CopyRect` BG→FG (:499-514); `ST_Drawer(fullscreen, refresh)` (:1108): `st_statusbaron = !fullscreen || automapactive` (fullscreen ⇔ `viewheight==200` from D_Display :252), `st_firsttime/refresh ⇒ full redraw else diff` (incremental widget-only). `ST_Responder` (:518) = the CHEAT layer — cheat sequences defined at st_stuff.c:400-470 (`iddqd` god, `idfa`/`idkfa`, `idmus<nn>`, `idsp*` choppers (commercial-gated), `iddt`, `idbehold*` powerup family (`cheat_powerup_seq[7]` :441, **`idclev<ep><map>` ⇒ `G_DeferedInitNew(gameskill, epsd, map)` :700-724 — a game-flow seam M9 must satisfy for the cheat to work**, `mypos`) + automap state tracking — message display is NOT here. **Messages live in `hu_stuff.c`: `HU_Ticker` (:505) pops `plr->message` into 1-line widget `w_message` (HU_MSGX/Y=0,0, `HU_MSGTIMEOUT=4*TICRATE=140` hu_stuff.h:44, `showMessages` cvar gate, `message_dontfuckwithme` for `HUSTR_FOUNDSECRET`-style priority lines :518-565); font = `STCFN%.3d` lumps `'!'`(33)…`'_'`(95), `HU_FONTSIZE=63` (hu_stuff.h:31-34, hu_stuff.c:392-414); `HU_Drawer` draws message + chat, map-title only over automap.** The brief's "ST_message/ST_saytime/LIFETIME" = these HU pieces (old-source names, §0.1); **no tips lumps exist anywhere in 1.10 (grep §0.1) — "tips" is a DOOM-2-retail-only feature, out of scope.**

0.9. **`wi_stuff.c` intermission (the real "level transition screen").** `WI_Start` (:1838) ← `G_DoCompleted`; `WI_initVariables` (:1796) clamps `maxkills/items/secret` to ≥1 (**divide-by-zero guard = the 100% denominator**) and `gamestate!=retail && epsd>2 ⇒ epsd-=3`. SP state machine `sp_state` (init :1318, update :1330): **1 pause(35) → 2 kills count-up (`cnt_kills += 2`/tic, `sfx_pistol` every 4th tic `!(bcnt&3)`, stop+`sfx_barexp` at `(skills*100)/maxkills`) → 3 pause → 4 items → 5 pause → 6 secrets → 7 pause → 8 TIME counts `+=3` toward `stime/35` AND par `+=3` toward `partime/35` (both clamp; done when both land) → 10 wait-for-accelerate ⇒ `WI_initShowNextLoc`** (or `WI_initNoState` commercial). Percent math is INTEGER DIVISION of the live counters; `cnt_pause = TICRATE` between stages. `WI_checkForAccelerate` (:1470): attack/use **edge** ⇒ `acceleratestage` (also the fast-forward: state≠10 jumps all counters to final + one `sfx_barexp`, :1338-1351). `ShowNextLoc` (:752-812): `cnt = SHOWNEXTLOCDELAY(4)*35` tics (:299), pointer blink `snl_pointeron = (cnt&31) < 20`, splats on maps `0..last` (+8 if didsecret) via `WI_drawOnLnode` (`lnodes[epsd][n]`, WISPLAT + WIURH0/1 flip), `WI_drawEL` "Entering <name>"; `NoState` (:730): `cnt=10` then **`G_WorldDone()`** — the whole next-level handoff. Music: `bcnt==1 ⇒ S_ChangeMusic(mus_inter)` (:1509-1514). Draw: `WI_drawStats` (:1436) slams screen1→0 background (`WI_slamBackground` :406, `bg` = `WIMAP<epsd>` patch at 0,0 :1538-1557), `WI_drawLF` (:421) `<WILV epsd,last>` + "Finished!" (WIF) at y=2 + 5/4-height spacing, labels at SP_STATSX/Y=50/50 (`WIOSTK`/`WIOSTI`/`WISCRT2`) with right-side `WI_drawPercent` = WIPCNT + `WINUM0-9` right-justified (`WI_drawNum` :669, 1994="n/a" skip, WIMINUS), TIME row at (16, 168) `WITIME` + `WI_drawTime` base-60 split (:683-717, `sucks` WISUCKS over 61*59), par `WIPAR` right half if epsd<3. **PRNG coupling: `WI_initAnimatedBack` (:503) draws `M_Random()%period` per ANIM_ALWAYS/ANIM_RANDOM anim at each state entry, AND `WI_updateAnimatedBack` (:532) draws `M_Random()%data1` again on every ANIM_RANDOM *loop* (wi_stuff.c:557-564) — episode-1 has NUMANIMS[0] animations; these share the MENU-stream `M_Random` with the statusbar face RNG (§0.8) — the ledger must count `ST_Ticker`'s draw while a level is live, or the animated intermission desyncs.** Sfx sites use `S_StartSound(0,…)` (world-less); silent-M9 keeps them as counters (§3).

0.10. **`f_finale.c` endgame.** `F_StartFinale` (:96): `gamestate=GS_FINALE`, `mus_victor`, **episodic ep1: `finaleflat="FLOOR4_8"`, `finaletext=E1TEXT`** — the text is a **compiled-in C string (d_englsh.h:359), there is NO F1TEXT endgame LUMP** (brief's "F1TEXT?" → measured absent from both source and WAD, §0.12; "lumps present-in-freedoom audit" = moot, the flat FLOOR4_8 + font + HELP2 are what must exist, all measured present). `F_Ticker` (:207): text phase until `finalecount > strlen(text)*TEXTSPEED(3) + TEXTWAIT(250)` ⇒ `finalestage=1` + `wipegamestate=-1`; commercial-only skip-after-50 + `gamemap==30 ⇒ F_StartCast` (bunny/cast = Doom-2/E3 scope, §4). `F_TextWrite` (:261): 64×64 flat tile flood of the WHOLE screen, then 1 char/tic-ish (`count=(finalecount-10)/3`) hu_font uppercase glyphs at (10,10), +11 y per \n. Stage 1 draw (`F_Drawer` :700): **shareware/registered ep1 ⇒ `HELP2` patch forever — no timeout, no key-exit: the ONLY way onward is opening the control panel (F-key/Esc via M_Responder, which runs in every state), or M_EndGame→`D_StartTitle`** — measured truth for "end screen → title" flow; `F_Responder` only eats events in stage 2. E1M8 exit is `ga_victory` from `G_DoCompleted` (§0.3) — never through `G_WorldDone`.

0.11. **Renderer additions M9 needs: `V_DrawPatch`, BG layer, view size.** `V_DrawPatch` (v_video.c:204-263): `x -= leftoffset; y -= topoffset`, per-column walk of `patch_t` (`{width,height,leftoffset,topoffset,columnofs[width]}`, r_defs.h:356-364; columns = `post_t {topdelta,length}` chains, 0xff-terminated, r_defs.h:285-292) copying raw palette bytes — **NO translation table and NO colormap parameter in this path; `CRANG`/TRANMAP do not exist in 1.10 (§0.1). Palette-level effects are done by `I_SetPalette` (gamma `usegamma` F11 + `ST_doPaletteStuff` damage/bonus/radiation palettes 1-8/9-12/13 st_stuff.c:69-74,1000), and view-only shading by the COLORMAP lump (r_data.c:639) — patches for menu/HUD/intermission are always full-bright raw indices.** Screens: `screens[0]`=FG, `screens[4]`=statusbar BG canvas (`#define BG 4` st_lib.h:33; 320×32 alloc ST_Init :1470); `V_CopyRect(BG→FG)` = the widget erase primitive. **View/statusbar interplay (the M4-renderer param FINDING): our port hard-codes fullscreen 320×200 (src/render/view.ts:60-66, "viewheight=SCREENHEIGHT"), and vanilla only shrinks the view via `R_ExecuteSetViewSize` (r_main.c:671-690): screenblocks 11 ⇒ `scaledviewwidth=320, viewheight=200` (no bar); 1-10 ⇒ `scaledviewwidth=setblocks*32, viewheight=(setblocks*168/10)&~7` (r_main.c:689-690; 10 ⇒ 168 — the brief's "200→112" is wrong at default; 112 corresponds to no vanilla block), `centery=viewheight/2`, `yslope` rebuilt (:730-734), `pspritescale=FRACUNIT*viewwidth/320` (:722); `R_InitBuffer` (r_draw.c:696-723): `viewwindowx=(320-width)>>1`, `viewwindowy = width==320 ? 0 : (200-32-height)>>1` — **at the DEFAULT screenblocks=10 the 320-wide view is TOP-aligned (viewwindowy=0) over a 168-high window and the bar simply owns rows 168-199**; the bar itself is never a viewport cut — `ST_Drawer(viewheight==200, …)` (d_main.c:252) just tells ST whether to paint itself. `R_FillBackScreen` (r_draw.c:731: GRNDSWICH/FLOOR4_8-pattern back fill + M_DOOM-less border) and `R_DrawViewBorder` (:832) only matter for windowed sizes.** `f_wipe.c:237/:262` melt for state changes (`wipegamestate` sentinel, d_main.c:216-222/:332-346) — runs in DISPLAY time (I_GetTime), NOT tics: intentionally non-deterministic, plan mirrors with rAF-time animation excluded from sim hashes.

0.12. **Freedoom specifics — measured on the pinned `wads/freedoom1.wad` this pass** (parsed with the repo's own `src/wad/wadfile.ts`, 3163 lumps; a first raw-node parse attempt mis-read the directory layout — corrected by using the tested loader): **THE WAD CONTAINS ALL 36 MAPS E1M1..E4M9** (Freedoom ships Phase 1/2/3 in one doom1-style IWAD) — so "Freedoom = single episode Phase 1" is a **policy choice for this port, not a data constraint**: the lever is the `gamemode` constant + `G_InitNew`'s shareware clamp to episode 1 (§0.4); with `retail`-like semantics the same code reaches E2+ (M12 corpus). E1 = maps 1-9 incl. E1M9 (the "Home" hub: E1M8 exit ⇒ finale, E1M9 exit ⇒ E1M4, §0.3). **Lump audit: ALL 1.10-required M9 graphics are PRESENT** — STTNUM/STYSNUM/STGNUM/STTPRCNT/STTMINUS, STKEYS0-5, STARMS, STFB0-3, STBAR, all 42 faces (46 `STF*` = 42 + STFB0-3), M_NGAME/M_OPTION/M_LOADG/M_SAVEG/M_RDTHIS/M_QUITG, M_EPISOD + M_EPI1-4, M_NEWG/M_SKILL/M_JKILL/M_ROUGH/M_HURT/M_ULTRA/M_NMARE, M_THERMO/M_THERM{L,R,M}, M_SVOL/M_SFXVOL/M_MUSVOL, M_PAUSE, M_DOOM, M_SKULL1/2; intermission WIMAP0 (+1,2), WILV00-08, WIA anims (58), WIURH0/1, WISPLAT, WIPCNT/WIF/WIENTER/WIOSTK/WIOSTI/WISCRT2/WICOLON/WITIME/WISUCKS/WIPAR/WIMINUS/WINUM0-9; TITLEPIC/CREDIT/HELP1/HELP2/ENDPIC/VICTORY2 present, FLOOR4_8 present; STCFN 33-96 + a few extras (all of `!`-`_` renderable, 68 lumps). **GOTCHA: the WAD also carries a Crispy-style SUPERSET menu set (M_VIDEO/M_WAD/M_CRISPY/M_KEYBND/…, 82 `M_*` lumps) — M9 must render the VANILLA menu graph, ignore the extras; also `M_EPI2-4` exist so the shareware-censoring (§0.7 M_Init) is code-side, and `D_READ_M`/`D_DM2TTL` (Doom-2 tracks) are ABSENT while `D_VICTOR` (finale) is present (music = M10).** No `CWILV*`/`INTERPIC` (correct for non-commercial). `F1TEXT` absent (never existed, §0.10).

0.13. **TS seams as-is (audited this pass).** `src/sim/game.ts`: `gTicker` reborn pass = D017 in-place deviation (:242-255), step 2 gameaction drain EMPTY (:251) — M9 turns both on; `src/main.ts`: 35 Hz accumulator (:183-194) + eventQueue + `attachPopInput`; `src/input/keyboard.ts`: `DEFAULT_EVENT_CODES` covers automap keys ONLY (:76-89) — menu keys (Esc/arrows/Enter/F1-F12/alphas) and the held-vs-event split audit are the keyboard-layer task; `src/render/rthings.ts:232/491` still excludes `KIND_MONSTER` (the ROADMAP M9-preview carry-over "production monster pixels"); `src/render/view.ts` fullscreen-only params (§0.11); `src/debug.ts` seams (`debugSim`, mouse injection :86) grow menu/HUD reads; e2e specs live in `e2e/*.spec.ts` with the `state()`/warp harness — zero-console-errors is already an assertion idiom.

## 1. Task graph and parallel waves

| Wave | Tasks (parallel) | Depends |
|---|---|---|
| 1 | M9-01, M9-02, M9-03 | M8 main |
| 2 | M9-04, M9-05, M9-06, M9-07 | M9-01, M9-02, M9-03 |
| 3 | M9-08, M9-09, M9-10 | M9-04..07 |
| 4 | M9-11, M9-12, M9-13 | M9-08..10 |

**Disjointness (one owner per path):** M9-01 owns `src/render/vvideo.ts` (new: patch decode/blit/BG-canvas/copyRect) + `src/wad/patches2.ts` (named-lump patch loader for UI lumps); M9-02 owns `src/input/keyboard.ts` + `src/input/menuMouse.ts` (menu-mode mouse→arrow synthesis, D-0yy) + their tests; M9-03 owns `src/sim/game.ts` (gameaction drain, gamestate per-tic routing, `gDeferedInitNew`, state object fields incl. `gamestate/gameaction/gamemap/gameepisode/gameskill/usergame/paused/viewactive`) + `src/sim/gamemode.ts` (the shareware-policy constant + clamps, §0.12) + `src/main.ts` (loop: advancedemo/M_Ticker placement, responder chain wiring, wipe trigger); M9-04 owns `src/ui/menu.ts` (+tests) — pure module over the M9-01 blitter, tables transcribed from m_menu.c, sfx via counted stub log; M9-05 owns `src/ui/statusbar.ts` + `src/ui/stlib.ts` (+tests) — STlib widgets + face machine + ST_Ticker/ST_Drawer onto the BG canvas; M9-06 owns `src/ui/humessage.ts` (+tests) — HU font + message queue + `showMessages`, and **fills the `player.message` string consumer** (the field exists, player.ts:145 — today drained nowhere); M9-07 owns `src/sim/wintermission.ts` (WI counters/state machine, sim-side) + `src/render/wiDraw.ts` (WI patches drawer); M9-08 owns `src/sim/reborn.ts` (G_DoLoadLevel re-setup path + G_PlayerReborn faithful body) + the death-flip tests; M9-09 owns `src/render/view.ts` params + `src/render/rthings.ts` KIND_MONSTER flip + `src/render/renderer.ts` (D_Display-order drawer composition incl. statusbar viewport + live-mobj sprites, ROADMAP M9-preview bullet 1); M9-10 owns `src/ui/finale.ts` (F_TextWrite/E1TEXT/FLOOR4_8/HELP2) + `src/ui/title.ts` (reduced attract: TITLEPIC page, any-key→menu); M9-11 owns `tests/render/m9-*.test.ts` L3 goldens; M9-12 owns `e2e/m9-flow.spec.ts` + `src/debug.ts` menu/HUD seams; M9-13 owns docs (`STATUS/ROADMAP/TASKS/JOURNAL/DECISIONS`) + golden re-bless ledger. `src/sim/hooks.ts` gains the sfx-stub/message counters as ONE additive edit (M9-03); `src/sim/random-sites.ts` follows the M8 rule: every draw site ships with its ledger line in the same commit (the ST_Ticker `M_Random` face draw, §0.8, is the new permanent stream addition).



## 2. Per-task specifications

### M9-01 — Patch blitter + screen layers (v_video)
- **Goal**: `vvideo.ts` implementing the v_video.c primitives M9 (and M12) build on: patch-header decode (`patch_t` r_defs.h:356-364, BE shorts + columnofs, posts r_defs.h:285-292 — reuse M1-06's `patch.ts` decoder where shapes agree), `vDrawPatch(x,y,scrn,patch)` verbatim column/post copy (§0.11, incl. the RANGECHECK warn-and-ignore behaviour :222-233), `vDrawPatchFlipped` (:265+), `vDrawPatchDirect` semantics (same pixels, no MarkRect), `vCopyRect` with BG(4)/FG(0) layers (§0.11), `screens[0]/[1]/[4]` equivalents (main canvas, wipe/back buffer, 320×32 statusbar canvas), `vMarkRect` no-op-or-AM-recompute, plus a `lumpPatch(wad, "NAME")` helper caching decoded patches by name (PU_STATIC equivalent, no eviction needed at 320×200).
- **Owns**: `src/render/vvideo.ts` (+ `vvideo.test.ts`), `src/wad/patches2.ts` (named-lump UI-patch table: STKEYS/STFST…/M_*/WI* name patterns from §0.12).
- **Must not touch**: `renderer.ts`, `view.ts`, `rthings.ts`, `src/ui/*` (consumers come in wave 2).
- **Source cites**: v_video.c:204 (V_DrawPatch)/:271 (Flipped)/:158 (V_CopyRect)/:143 (V_MarkRect), r_defs.h:285-292/:356-364, st_lib.h:33-34, st_stuff.c:499-514.
- **Acceptance**: 1) L1: every §0.12-audited UI lump decodes (header sanity: w/h/offsets ≥ sane, posts walk terminates with 0xff on ALL columns) on freedoom1.wad (skipIf) — golden hash of decoded pixels for a 10-lump spot set; 2) synthetic-fixture parity test: blit vs patch.ts reference decode byte-exact incl. left/topoffset anchor math; 3) `vCopyRect` BG→FG erase restores canvas-exact background (roundtrip test); 4) out-of-bounds patch returns without throwing, warning counted.
- **Verify**: `npx vitest run src/render/vvideo.test.ts src/wad/patches2.test.ts`

### M9-02 — Keyboard event layer + menu mouse
- **Goal**: extend the event seam (§0.13) to the full menu/game-flow key set: `DEFAULT_EVENT_CODES` + Esc/Enter/Backspace/arrows/F1-F12/a-z/0-9/`-`/`=` mapped to vanilla `KEY_*`/ASCII data1 (doomdef.h enum order), keeping the held-channel split (movement keys stay polled); implement `menuMouse.ts`: when the menu layer is armed, hover/wheel/click synthesize `KEY_UPARROW/KEY_DOWNARROW/KEY_ENTER` keydown+keyup pairs (the OS-layer trick 1.10 relied on, §0.7 mouse finding) with item-height=16 math from the caller's item boxes; keep keyup-returns-false semantics.
- **Owns**: `src/input/keyboard.ts` (additive table + tests), `src/input/menuMouse.ts` (+tests); `src/input/mapping.test.ts`/`keyboard.events.test.ts` extensions.
- **Must not touch**: `src/main.ts` (M9-03 wires), `src/ui/*`, `src/sim/*`.
- **Source cites**: doomdef.h KEY_* codes; m_menu.c:1349-1710 (what must be deliverable); g_game.c:571-577 (keyup never eaten); ROADMAP M9 exit line ("menus keyboard+mouse operable (L4 real clicks/keys…)") — QUOTE: “menus keyboard+mouse operable (L4 real clicks/keys, zero console errors)”; ROADMAP M10: “SFX with priority/attenuation/panning … volume settings take effect (L4)” ⇒ sfx *bodies* are M10, silent-M9 is the roadmap boundary (§3 D-0xx); M11: “bindings/sensitivity/volumes persisted across reload” ⇒ option *persistence* is NOT M9.
- **Acceptance**: 1) every key M_Responder consumes emits `{type,data1}` packets with vanilla-identical data1 (table test vs doomdef.h transcribed constants); 2) F-keys reach the queue (they are event-only, never held); 3) mouse-synth unit test: arming with 6 item boxes + hover row 3 ⇒ down×N to index, click ⇒ Enter pair; wheel = ±1; 4) no regressions in `keyboard*.test.ts`.
- **Verify**: `npx vitest run src/input`

### M9-03 — gameaction drain + gamestate routing + deferred-init plumbing
- **Goal**: complete `gTicker` steps 1-2 (§0.2) behind real state fields: `GameState.gamestate: GS_LEVEL|GS_INTERMISSION|GS_FINALE|GS_DEMOSCREEN`, `gameaction`, `secretexit`, `wminfo` carrier; `gDeferedInitNew(skill,ep,map)`/`gExitLevel`/`gSecretExitLevel`/`gWorldDone` exported setters; per-state ticker routing (GS_LEVEL → today's body; GS_INTERMISSION → M9-07 module hook; GS_FINALE → M9-10 hook; GS_DEMOSCREEN → title hook) — each via registrable module hooks (M8 self-import idiom) so later tasks do not re-edit game.ts; `gamemode.ts` pinning the shareware-episodic policy constant + G_InitNew clamps (§0.4); `src/main.ts`: tic-block runs advancedemo-flag + M_Ticker (§0.6), display block consumes `wipegamestate` sentinel via the renderer seam, `paused` gate wired to the existing pause plumbing; `hooks.ts` additive: `sfxStub(name)` counter (M10 replaces body) + `gameactionLog`.
- **Owns**: `src/sim/game.ts` (+`game.test.ts`), `src/sim/gamemode.ts`, `src/main.ts`, `src/sim/hooks.ts` (additive-only).
- **Must not touch**: `vvideo.ts`, `keyboard.ts`, `src/ui/*` (hooks are interface-first: `GameFlowHooks` in `game.ts`), `wintermission.ts`.
- **Source cites**: g_game.c:605-650/:1333-1357, d_event.h:55-65, d_main.c:354-398 (tic block), d_net.c:636 (tic sourcing mapping note), d_main.c:216-222 (wipe sentinel).
- **Acceptance**: 1) `gDeferedInitNew(2,1,1)` from GS_DEMOSCREEN drains to level load in ONE gTicker call (while-loop semantics, with a guard-counter mirroring M8's unimplementedSpecial idiom for unregistered state handlers); 2) hash stability: a fresh `gInitGame` E1M1 2000-tic run byte-equals the M8 blessed goldens (no flow fields touched → zero re-bless); 3) `gameaction` never survives a completed `gTicker` (assert); 4) clamps: ep=5→1 (shareware), map=0→1, map=12→9, skill=6→5; 5) wipe sentinel consumed exactly once per state change.
- **Verify**: `npx vitest run src/sim/game.test.ts src/sim/gamemode.test.ts`



### M9-04 — Menu stack + screens
- **Goal**: `menu.ts` = verbatim m_menu port for our scope: tables (Main 6 / Epi 4 / Skill 5 / Options 8 / Sound 2 thermo / Read 1-2) with `status/alphaKey/prevMenu/x/y/lastOn` (§0.7), `mSetupNextMenu`/back via prevMenu chain (NO push/backup stack — §0.1), `mResponder` key map byte-for-byte incl. skip-status-`-1` cycling and alphaKey wrap search, `mTicker` skull (8-tic), `mDrawer` (routine + items at 16px pitch + skull `x-32, y-5+itemOn*16`, centered y=100 message path), `mStartMessage/StopMessage` with the `'n'/'y'/' '/ESC` input gate, `M_Init` shareware censors (EpiDef.numitems 4→3 + `M_Episode` ad-message block), `M_ClearMenus`→`D_StartTitle` path for F7-EndGame-yes and the finale dead-end (§0.10), sfx sites → `hooks.sfxStub` keys (`sfx_pstop/sfx_stnmov/sfx_swtchn/sfx_swtchx/sfx_pistol`, quit `quitsounds[(gametic>>2)&7]`); entries for load/save/quicksave M11 slots (registered, message "not yet"). Any-key-on-title ⇒ menu via the G_Responder-branch seam from M9-03.
- **Owns**: `src/ui/menu.ts` (+ `menu.test.ts`), `src/ui/menuData.ts` (lump-name/key tables).
- **Must not touch**: `game.ts` (consumes M9-03 hooks), `vvideo.ts` (calls only), `statusbar.ts`, `main.ts` (M9-03 imports `menu.mResponder/mTicker/mDrawer` at wiring time).
- **Source cites**: m_menu.c:138-169/:250-257/:284-332/:352-376/:856-946/:1349-1710/:1723-1846/:1847-1893 (M_Init), st table positions in §0.7.
- **Acceptance**: 1) fixture: Main→NewGame→(epi censor: 3 items shareware)→Skill; ↓ cycles 0..n-1 skipping status −1; alphaKey 'u' jumps to Ultra from item 0 (wrap search); 2) Enter on skill 4 (nightmare) ⇒ message armed, only y/YES fires `gDeferedInitNew(5,epi+1,1)`; n/ESC cancels; 3) Backspace Epi→Main, lastOn restored; Esc closes (menuactive=false) + `sfx_swtchx` counter; 4) skull toggle at tics 10,18,26…; 5) draw-order golden: Main screen frame hash + L5 PNG (D016) of title→menu.
- **Verify**: `npx vitest run src/ui/menu.test.ts`

### M9-05 — Statusbar: widgets + face machine
- **Goal**: `stlib.ts` (initNum/initPercent/initMultIcon/initBinIcon + update semantics incl. 1994-skip, zero-draws-`0`, right-justified LSB-first digits, minus at x−8, BG-erase-box via vCopyRect, percent-sign refresh-only, §0.8) + `statusbar.ts`: lump load (§0.8 lump list — all present per §0.12), widget table at the exact §0.8 coordinates, `ST_Ticker` (clock, **`mRandom()` face draw every tic — new random-sites ledger line `st_face`**), `ST_updateFaceWidget` full precedence machine incl. the confusing turn-direction `i` branch verbatim (§0.8), `ST_calcPainOffset` memo, `ST_drawWidgets(refresh)`, `ST_Drawer(fullscreen, refresh)` + `ST_refreshBackground` (STBAR+faceback→BG→FG), `ST_doPaletteStuff` hooked to the existing palette-band module (`ppalette.ts`, M7-06 — STARTREDPALS 1-8/STARTBONUSPALS 9-12/RADIATIONPAL 13 gates verbatim), cheat layer ST_Responder deferred (§4).
- **Owns**: `src/ui/stlib.ts`, `src/ui/statusbar.ts` (+ both tests), `src/sim/random-sites.ts` (`st_face` key).
- **Must not touch**: `humessage.ts`, `menu.ts`, `renderer.ts` (M9-09 composes; M9-05 exports `stDrawer/stTicker/stStart`), `player.ts`.
- **Source cites**: st_stuff.c:78-250 (consts)/:497-514/:733-918 (face)/:924-986/:988-998/:1000-1052 (palette)/:1054-1122/:1124-1197 (lumps)/:1282-1444 (widgets); st_lib.c:66-293.
- **Acceptance**: 1) face-table unit: for each state combo (dead/evilgrin/attacked-ouch/attacked-turnL/R/rampage-self/rapid/god/idle) assert exact `faceindex` incl. pain-offset tiers 0/8/16/24/32 with boundaries health∈{80..100}→0, {60..79}→8, {40..59}→16, {20..39}→24, {1..19}→32 (formula `((100−h)*5)/101` truncation, health clamped ≤100); turn-direction branch tested at ±46°/±180° attacker angles; 2) `st_face` ledger: N-tic run counts exactly N `mRandom` draws; 3) keys widget: cards[1]+cards[4] ⇒ keyboxes[1]=4 (blue+lava = STKEYS4); 4) 1994: fist ⇒ ammo digits erased, `0` health draws single STTNUM0; 5) L3 golden: statusbar frame (fixture WAD + scripted weapon/health/key changes, IWAD skipIf variant) + face-strip (5 tiers) L5 PNG.
- **Verify**: `npx vitest run src/ui/stlib.test.ts src/ui/statusbar.test.ts`

### M9-06 — Messages (HU) + map title
- **Goal**: `humessage.ts`: `STCFN033-095` font load (§0.12 coverage), `w_message` 1-line widget (HU_MSGX/Y 0,0), `huTicker`: pop `plr.message` (first consumer — pickups/secret/cheats already write it, player.ts:145), 140-tic timeout, `showMessages` gate (F8 toggles — owner of the flag is M9-05? NO: menu owns F8 handler here, flag lives here, cite §0.7 F8), `message_dontfuckwithme` priority path for the secret line (`HUSTR_FOUNDSECRET` — "A secret was revealed!" transcribed from d_englsh.h), chat widget = registered no-op (§4); `HU_Erase` semantics = redraw behind (canvas recompose); map-title line (`HU_TITLE` episodic = `mapnames[(ep-1)*9+map-1]` copied verbatim into `w_title` at `HU_Start` — one per level start, hu_stuff.c:472-478, automap-only draw :486-492).
- **Owns**: `src/ui/humessage.ts` (+test), `src/sim/mapnames.ts` — transcribe the compiled `mapnames[]` array (hu_stuff.c:115-167, episodic `HU_TITLE = mapnames[(ep-1)*9+map-1]` hu_stuff.c:49; the strings are id's vanilla titles, compiled-in, NOT WAD lumps — the Freedoom map-title truth: they render vanilla names; a FINDING note goes in the impl report, and the exit task records whether the D016 review accepts "E1M1: Negative Zone" (Freedoom) vs "E1M1: Hangar" (vanilla string) — data-vs-code truth: 1.10 code has vanilla strings only).
- **Must not touch**: `statusbar.ts` (message is a SEPARATE overlay onto screens[0] FG, never the bar — §0.8), `menu.ts` (chat/menu-message split: M_StartMessage belongs to menus).
- **Source cites**: hu_stuff.c:505-535/:392-414/:434-438/:486-503, hu_stuff.h:38-44, hu_lib.c initSText/addMessageToSText (:76-/:137 — clear-row via BG-erase id), d_englsh.h (HUSTR_ strings).
- **Acceptance**: 1) message set during a tic ⇒ visible next 140 tics, replaced messages append (multi-message queue = widget line-count 1 ⇒ newest shown, older rows scrolled per hu_lib scrollTextLine), timeout clears; 2) showMessages=false ⇒ not drawn BUT secret-message priority still queued per :518-530; 3) golden: 3-frame strip (message appear/scroll/expire) + secret-priority frame; 4) zero DOM writes outside the canvas (console-error gate).
- **Verify**: `npx vitest run src/ui/humessage.test.ts`

### M9-07 — Intermission (WI) counters + pages
- **Goal**: `wintermission.ts`: `wiStart/wiTicker/wiUpdateStats` sp_state 1→2→3→4→5→6→7→8→10→ShowNextLoc→NoState with integer-division percents, +2/+3 steps, 4-tic `sfxStub('sfx_pistol')` dots, cnt_pause=35, accelerate edges from `cmd.buttons` (M9-03 field), `G_WorldDone` hook at NoState end; `lnodes`/anims tables for epsd0 transcribed (wi_stuff.c:177-288); ANIM_RANDOM `mRandom` draws ledgered (`wi_anim` key); `wiDraw.ts`: WIMAP0 slam, drawLF/drawEL/drawOnLnode (splat/WIURH blink)/drawStats/drawTime (base-60 + WISUCKS)/par (pars E1 row §0.3 — sim-side constant from M9-03's wminfo), `WI_Responder` no-op.
- **Owns**: `src/sim/wintermission.ts` (+test), `src/render/wiDraw.ts` (+test), `src/sim/random-sites.ts` (`wi_anim`).
- **Must not touch**: `game.ts` (registers via hook), `menu.ts`, `statusbar.ts`.
- **Source cites**: wi_stuff.c:406-500/:503-580/:669-724/:730-812/:1318-1467/:1470-1536/:1538-1706/:1796-1850, §0.3 routing table.
- **Acceptance**: 1) scripted tally: 50/33/0% kills/items/secrets at known counters → state sequence & per-tic digit frames exact (golden hash of counter SoA), 2) accelerate: attack edge at state 2 ⇒ all counters final + `sfxStub('sfx_barexp')` ×1 → ShowNextLoc; 3) E1M8 exit (from G_DoCompleted) does NOT reach WI (regression pin vs §0.3), E1M9 ⇒ next=3 (E1M4); 4) draw goldens: StatCount frames at t=1/70/200, ShowNextLoc blink-on/off, L5 montage; 5) NoState end ⇒ `gWorldDone()` observed once.
- **Verify**: `npx vitest run src/sim/wintermission.test.ts src/render/wiDraw.test.ts`

### M9-08 — G_DoReborn full restart (D017 retirement) + level-transition plumbing
- **Goal**: faithful single-player death = `ga_loadlevel` (§0.5): complete `gDoLoadLevel` (re-run map decode→`pSetupLevel` equivalent via the gInitGame internals WITHOUT resetting `GameState` identity: gametic/leveltime semantics per g_game.c:445-503 incl. `wipegamestate` flag, PST_DEAD→PST_REBORN, frags memset, input-layer `clear()` mirroring :490-497), faithful `gPlayerReborn` (preserve frags/kill/item/secretcount across full clear, pistol start §0.5) replacing the in-place body, and `gExitLevel/gSecretExitLevel → ga_completed → G_DoCompleted` (wminfo fill incl. pars §0.3, E1M8 ⇒ `ga_victory`) + `gWorldDone/G_DoWorldDone`. The reborn pass consumes `PST_REBORN` via `G_DoReborn` — the game.ts deviated comment block (:242-249) is deleted with a `docs/DECISIONS.md` D017→"retired at M9" flip (exit task transcribes; M9-08 files the draft).
- **Owns**: `src/sim/reborn.ts` (+ `reborn.test.ts`), edits `src/sim/game.ts` reborn pass + gameaction cases (post-M9-03), `src/sim/pplayer.ts` (G_PlayerReborn body if it lives there — M7-03 territory, smallest diff).
- **Must not touch**: `p_mobj.ts` spawn core (calls only), UI modules, `main.ts` (input clear via exported `keyboard.clear()` seam from M9-02/03).
- **Source cites**: g_game.c:445-503/:613-614/:779-796/:800-848/:924-932/:1002-1190, DECISIONS.md D017, JOURNAL:221.
- **Acceptance**: 1) scripted death mid-E1M1 ⇒ NEXT-LEVEL observables: taken pickups gone→back (item count in world state), killed monsters re-spawned (mobj census == fresh), player at start with 50 clips, `killcount/itemcount/secretcount` PRESERVED across the restart (fixture: kill 3, die, assert counters); 2) flip list executed: `e2e/weapons.spec.ts` death test + `tests/headless/m8-pacing.test.ts` (400000-tic no-death theorem re-derives? its reborn assumptions — if any envelope crossed death, re-derive bounds in-file), `tests/headless/monsters.test.ts`, `p_ammo/p_pspr/pplayer/pswitch` reborn readers audited (grep list §0.5) — each either passes unchanged or gets a `// D017 retired` pin update; 3) motion goldens containing a death re-blessed ONCE with reason "D017 retirement" (expected: none of the M5-M7 movement goldens cross death — verify, don't assume); 4) exit flow: scripted E1M1 exit ⇒ GS_INTERMISSION, tally ⇒ E1M2 live, `leveltime` reset, `viewactive=true`.
- **Verify**: `npx vitest run src/sim/reborn.test.ts && npx vitest run tests/headless/m8-pacing.test.ts`

### M9-09 — Renderer: statusbar viewport + live-mobj sprites + D_Display order
- **Goal**: `view.ts` gets the `R_ExecuteSetViewSize` params (screenblocks 3-11 ⇒ `scaledviewwidth/viewheight=(sb*168/10)&~7`, `viewwindowx/y` r_draw.c:696-723, yslope/centering rebuild, `pspritescale`), default screenblocks **9 per m_misc.c:279 shipped default** (windowed 288×144 + borders, the classic look; `=`/`-` live-resize); borders when `scaledviewwidth<320`: `R_FillBackScreen` FLOOR7_2 tile + `brdr_*` patches (§0.12: all present) + `R_DrawViewBorder` 3-count erase; `renderer.ts` composes the D_Display draw order (§0.6) with state switch, statusbar BG-canvas composite (`viewheight==200 ⇒ fullscreen flag`), and the **live-mobj sprite pass flip**: rthings `KIND_MONSTER` stop-excluded, real sprite frames via states.ts + the M4 8-rotation/clipping (D018 flips: strips switch to production pixels — one re-bless of the three m8-* strips with reason).
- **Owns**: `src/render/view.ts` (params), `src/render/rthings.ts` (flip), `src/render/renderer.ts`, `src/render/borders.ts`, `src/sim/p_mobj.ts` **read-only consumers via existing exports** (no edit), `tests/render/renderer.test.ts`.
- **Must not touch**: `vvideo.ts` internals (import), `wintermission.ts`, `src/sim/*`.
- **Source cites**: r_main.c:671-757, r_draw.c:696-723/:731-830 (FillBackScreen/ViewBorder) + brdr loop :773-790, d_main.c:193-330, rthings.ts:232 note, D018.
- **Acceptance**: 1) viewheight table test: sb∈{11,10,9,6,3} ⇒ exact 200/168/144/96/48 + windowx/y exact (r_draw math); 2) HOM=0 + no-single-color at 4 sampled viewpoints sb9 with bar (L3 goldens, E1M1); 3) monsters render: E1M1 1280-thing census → killable set visible, sprite-correctly clipped vs walls (fixture mobjs); m8 strips re-bless with production pixels (D018 flip note in meta.json); 4) `state().render.hom == 0` across the sb sweep.
- **Verify**: `npx vitest run src/render/view.test.ts src/render/renderer.test.ts tests/render/motion.test.ts`

### M9-10 — Title (reduced attract) + finale endgame
- **Goal**: `title.ts`: boot → `GS_DEMOSCREEN` with `D_PageTicker`-shaped `pagetic=170` page + TITLEPIC patch draw (D_PageDrawer), `advancedemo` ⇒ TITLEPIC-only cycle (no demos, §0.6 boundary), `gDeferedPlayDemo` stub counter, any-key/any-mouse-button ⇒ `mStartControlPanel` (G_Responder branch port in the wiring seam — game.ts hook from M9-03); `D_StartTitle` equivalent = demosequence reset + TITLEPIC (F7-EndGame-yes + Quit-yes targets; Quit-yes in browser = `I_Quit` ⇒ page-into-title, not process-exit — dev D-0zz). `finale.ts`: `F_StartFinale` episodic ep1 (mus stub, `FLOOR4_8` full-screen tile, E1TEXT transcription from d_englsh.h:359 verbatim, TEXTSPEED/TEXTWAIT math, cursor (10,10) +11/newline), stage1 ⇒ `HELP2` patch forever + `wipegamestate=-1`, `ga_victory` case wired (M9-03), commercial/cast branches absent (§4).
- **Owns**: `src/ui/title.ts`, `src/ui/finale.ts` (+ tests), `src/ui/textdata.ts` (E1TEXT + quoted strings).
- **Must not touch**: `menu.ts` (calls `mStartControlPanel` export), `wiDraw.ts`, `vvideo.ts`.
- **Source cites**: d_main.c:423-529, g_game.c:529-541, f_finale.c:96-254/:256-325/:700-737, d_englsh.h:359.
- **Acceptance**: 1) boot shows TITLEPIC; first keypress arms menu (verified in e2e); 2) E1M8 exit ⇒ finale: text reveal at exact tics (`strlen*3+250`), wipe, HELP2 held; F10→quit-yes ⇒ title; 3) E1TEXT byte-transcription test (hash vs C-string literal incl. `\n` count); 4) goldens: title frame, finale t=200 text frame, HELP2 frame; L5 montage (D016).
- **Verify**: `npx vitest run src/ui/title.test.ts src/ui/finale.test.ts`

### M9-11 — L3 golden corpus: HUD/menu/intermission
- **Goal**: `tests/render/m9-*.test.ts` — scripted scenarios (synthetic WAD variants + IWAD skipIf): statusbar 8-variant matrix (health tiers × face states × keys × weapons), message strip, title/menu 3 screens, intermission tallies (0%/50%/100% + par/sucks), finale 3-phase, sb9/sb11 viewport pair; all hashed off the SAME rAF-frame driver as M4-M8 goldens.
- **Owns**: `tests/render/m9hud.test.ts`, `m9menus.test.ts`, `m9wi.test.ts`, `tests/fixtures/m9Scenarios.ts`.
- **Must not touch**: `src/**`.
- **Acceptance**: goldens committed via one `goldens:update` run with reason list; IWAD-skip behavior matches existing pattern; HOM=0 pins on every 3D+bar frame.

### M9-12 — L4 e2e: the whole loop with real keys/mouse
- **Goal**: `e2e/m9-flow.spec.ts`: TITLEPIC → real Esc → real ↓/Enter skill=Hurt me → play → menu via Esc mouse-synth click → Options thermo ← → size `=` → statusbar live during play (face pixel probe) → scripted exit route (reuse M6 route harness) → intermission accel-click → E1M2 loads → die → level restart (D017 observables) → F7/endgame-yes → title. Zero console errors throughout; `state().hash` equality checks at the seams.
- **Owns**: `e2e/m9-flow.spec.ts`, `src/debug.ts` seams (menu/HUD reads, additive), `playwright.config` only if a second worker id is needed.
- **Must not touch**: `src/ui/*` (seams already attached), sim internals.
- **Acceptance**: green on CI headless; screenshot artifacts land in the artifact dir for the exit review; any flake gets a re-run pin recorded, not a sleep.

### M9-13 — Exit sweep + docs (D017 retirement, D018 flip, silent-M9 ledger)
- **Goal**: cross-suite green (`npm run check && npm run e2e && goldens --check`), re-bless ledger (each reason once), random-sites final reconciliation (`st_face`/`wi_anim` included — the ST_Ticker draw shifts ALL menu-stream numbers: WI anim timing asserts in M9-07 use the LIVE stream, not a fresh one), docs: DECISIONS (D017 retired, D018 flipped, D-0xx silent-M9, D-0yy mouse-synth, D-0zz title-as-quit-target), ROADMAP M9 `[x]` + M10 preview, JOURNAL narrative + FINDINGS, STATUS; L5 montage pack for the D016 human-eyes gate (title, menus, bar matrix, intermission, finale, monster-pixels strip).
- **Owns**: `docs/*` + goldens:update invocations.

## 3. Deviations, deferred hooks, and open decisions

- **D-0xx (draft, M9-04/05/07) silent-M9 sfx policy**: every `S_StartSound/S_ChangeMusic/S_Stop` site lands as `hooks.sfxStub(name)` (counted, deterministic, no audio). Boundary QUOTE — ROADMAP M10 exit: "SFX with priority/attenuation/panning (L1 mixer unit tests + offline-mix golden buffers); SMF music plays in e2e…" ⇒ sound *bodies* are M10 by plan; M9 keeps every call SITE at its vanilla address so M10 fills the stub in place. Face/intermission PRNG unaffected (sfx calls burn no `P_Random`).
- **D-0yy (draft, M9-02) menu mouse via synthesized arrows**: vanilla 1.10 has no `ev_mouse` consumer in menus (§0.7 — the OS layer faked keys); the ROADMAP demands "keyboard+mouse operable", so the browser layer synthesizes key events — a documented input-stack deviation, zero sim/menu deviation.
- **D-0zz (draft, M9-10) quit-yes ⇒ title**: browser sandbox has no process exit; `I_Quit` maps to title reset (visible, testable). Demo playback (`.lmp`) stays Post-M12 stretch (ROADMAP §Stretch) — attract screen is TITLEPIC-only (§0.6).
- **D017 retirement** (§0.5/M9-08) and **D018 flip** (§0.13/M9-09) are this milestone's docs events; DECISIONS.md flips land with M9-13.
- **Deferred (registered stubs, counted)**: save/load/quicksave + options persistence + F6/F9 + sound-volume *effects* → M11; cheats `ST_Responder` cheat block (incl. idclev — the plumbing it needs exists by M9-03, cheap to enable in M9-13 stretch slot) → M11 test-suite pairing; chat widget/`HU_Responder` chat keys → M11 (multiplayer-adjacent); deathmatch/netgame intermission screens (`WI_updateDeathmatchStats`/`NetgameStats`, frags widgets) → never-single-player-reachable, fixture-transcribed only; commercial/Doom-2 finale branches (cast order, bunny scroll, C1-C6 texts) + `D_DoAdvanceDemo` demos + `gamemode!=shareware` screens (CREDIT/ENDPIC/VICTORY2 draws) → code-shipped-but-unreached in the pinned-WAD policy (§0.12), M12 corpus decides; episode 2-4 flows ⇒ M12 (data present, policy shareware-clamped).
- **Open decision**: screenblocks default — this plan pins the SOURCE truth 9 (m_misc.c:279) and the L5 pack shows both 9 and 11 for the D016 review; if the review prefers "classic bar-full" semantics the default flips to 10 with a one-line change + golden re-bless (documented outcome, not silent).

## 4. Exit criteria (milestone-level, objectively checkable)

1. **Full flow, real input, zero console errors**: e2e TITLEPIC → skill menu → play → options → exit → tally → E1M2 → death→restart → endgame→title (§0.6/0.9/0.10); L4 spec green (`e2e/m9-flow.spec.ts`).
2. **Menus** keyboard- and mouse-operable (synth D-0yy) with skull animation, thermos, alphaKeys, message prompts; menu screens L3-goldened.
3. **Status bar** (sb9 default window + sb11 check): widgets/face-machine goldens incl. all 42 faces reachable via scripted state; `st_face` PRNG ledger green.
4. **Intermission**: tally math goldens (percent integer-division exact), accelerate works, E1M9→E1M4 and E1M8→finale routing pins.
5. **D017 retired**: death restarts the level (respawn census + preserved counters, §0.5) — DECISIONS.md updated; deviated-observable pins flipped.
6. **Production monster pixels**: KIND_MONSTER rendered/clipped in a live level golden, m8 strips re-blessed off production pixels (D018 flipped).
7. **`npm run check && npm run e2e` + goldens --check green** on integrated main; re-bless reasons ledger exactly as itemized; no new console errors.
8. **D016 pack**: title/menu/bar/intermission/finale/monster montage reviewed in the exit turn.
9. **Docs**: ROADMAP M9 `[x]`, JOURNAL narrative + surprises, TASKS rows linked, DECISIONS flipped/added.

## 5. Final report

(filled at exit by M9-13; wave table below is the plan-of-record)

| Wave | Tasks | One-liner |
|---|---|---|
| 1 | M9-01 M9-02 M9-03 | Patch blitter + BG layers; full key-event layer + menu mouse; gameaction/gamestate plumbing (shareware policy) |
| 2 | M9-04 M9-05 M9-06 M9-07 | Menu stack; statusbar + face machine; HU messages; intermission counters/pages |
| 3 | M9-08 M9-09 M9-10 | G_DoReborn full restart (D017) + transitions; statusbar viewport + monster pixels; title + finale |
| 4 | M9-11 M9-12 M9-13 | L3 golden corpus; L4 whole-loop e2e; exit sweep + docs/ledgers |

