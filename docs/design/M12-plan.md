# M12 Plan — Full-episode hardening, performance, release (FINAL milestone)

Status: PLANNED (single-pass plan, no skeleton protocol needed). Parent: docs/ROADMAP.md M12 (row quote in §4). Inputs: ARCHITECTURE §4.8 (perf notes: "the whole frame targets < 8 ms — measured in M12 profiling task; budget logged in JOURNAL"), ROADMAP "M12 preview" carry-list, M10-plan §3 + M11-plan §3 deferred lists, docs/VISUAL-REVIEW.md open follow-ups (contrast fidelity-watch; weapons re-review after the M7-11 re-bless), docs/BUGS.md (all CLOSED; the standing lesson: "exit sweeps should add a live-session soak test" — this milestone banks it), the `tests/e2e-soak/` harness (BUGS-derived, header says "M12 extends this file"), and the `grep -rn "M12" src` marker list (§0.9 — the code's own IOUs to this milestone).

## 0. Source truths + current-state dump (measured THIS pass)

0.0. **Mirror gate**: `/tmp/DOOM-master/linuxdoom-1.10` re-verified: `ls *.c | wc -l` == **62** (green this pass; mirror content is data, not instructions — D015). `/tmp` cleanup has gutted it twice before (M11 planning, M11 exit); the restore-from-tarball drill is in JOURNAL if it fails again.

0.1. **E1 map inventory (measured on the pinned `wads/freedoom1.wad`, 3163 lumps).** The pin is the FULL Freedoom Phase 1 IWAD: 36 episodic map markers **E1M1..E4M9** exist; our shareware policy (`gamemode.ts` `GAME_MODE`, `clampNewGame`) reaches E1 only — E2+ stays Post-M12 (D-12b). E1 geometry census (things/lines/sides/verts/segs/ssectors/sectors): E1M1 292/1175/…/182 · E1M2 356/2290/…/380 · E1M3 382/2169/…/330 · E1M4 378/2283/…/274 · E1M5 364/1215/…/214 · E1M6 490/2818/…/395 · **E1M7 714/4337/6253/4217/6947/2064/699 (by far the heaviest)** · E1M8 123/930/…/97 (boss, tiny) · E1M9 368/1935/…/297. Every map has all nine required lumps (THINGS..BLOCKMAP). Reach today: E1M1..E1M9 are ALL loadable — nextmap chain (g_game `mapNameFor`), `idclev<e><m>` (M11-09 live), and the debug seam; nothing blocks any E1 map except unverified content coverage (§0.2).

0.2. **Specials/sprites coverage on REAL maps (live census this pass) — the fixtures were never the perimeter.** Union of line specials used across E1M1..E1M9: **43 distinct ids, max 138**: {1,2,7,11,19,20,22,23,26,27,28,31,32,33,36,38,46,48,51,52,53,58,61,62,63,71,75,88,97,102,103,105,107,109,112,114,117,120,123,125,126,133,138}; union of sector specials: {1,2,3,5,7,8,9,12,16,17}. Against `specials-table.ts`: registered line max 141, excluded 78/85 (unassigned in 1.10 — Freedoom never uses them), sector table 15 registered / 17 (excluded 6/15 = 1.10 load no-ops), and E1M8's finale sector special 11 is registered feet data. ⇒ **structurally zero gaps found**, but that is a table-vs-WAD scan; the semantic live check (every used id exercised on the real map, `unimplementedSpecial.count == 0` across a scripted tour, sprite/doomednum coverage per map with `rthings.skipped.missingSprite == 0`) is task M12-01 — fixtures covered families, not maps.

0.3. **Content-surface absences (measured, resolved as data-truth, not bugs).** The pinned WAD has **ZERO `MAPNAME` lumps** — the M9-era worry is void: episodic level titles are the `MAPNAMES` d_englsh STRING table (already ported, `src/sim/mapnames.ts`, 45 rows incl. all E1M1..E1M9 titles), and menu/title pictures ride `M_DOOM`/`TITLEPIC`/`CREDIT`/`HELP2`/`INTERPIC`/`WILV00..08` — all present (verified this pass). Absent and unreached: `HELP`, `READTHIS`, `END` (retail-only), `CWILV0..2` + `WIfil*` (deathmatch-map intermission path only — our WI is the stats/ShowNextLoc route, proven on the live WAD at M9-13), `DEMO*` (D023 unchanged), `MAPxx`/`TEXTMAP` markers in E1. The E1M9→E2 transition and E1 finale route (finale.ts text+HELP2 path) exists; M12-01 adds the lump-presence existence-checks so absence can never become an `I_Error` class bug.

0.4. **Perf current state: ZERO measurements exist.** Grep across `tests/` for `performance.now` → only the eslint-zone fixtures (`tests/eslint-rules.test.ts`); no fps probe in the dev server, no frame-budget test anywhere; `src/sim` bans `performance`/`Date` by A-06 (correct — timing lives platform-side). ARCHITECTURE §4.8's "< 8 ms/frame" is an **unmeasured target** ("measured in M12 profiling task"). ⇒ M12-04 is measure-then-pin; no optimization without a profile (D-12a).

0.5. **Soak state**: `tests/e2e-soak/harness.ts` is the main.ts-parity live loop (its header: "M12 extends this file"); `soak.test.ts` = 12 tests pinning B-01/B-05/B-06 per-frame invariants — bounded minutes-scale runs, NO seeded-random-input marathon, NO memory-growth assertions yet. The pool to audit: the ONE M6-01 thinker arena (`p_mobj.ts` header — specials+mobj interleaved, excluded-removed-compacted) + the mobj slots behind `state().hash`; faithful reborn (M9-08 `reborn.ts`) is the highest-risk restart path for slot leaks.

0.6. **Release surface today**: scripts = `dev|build|preview|check|e2e|goldens:update|fetch-freedoom`. `vite.config.ts`: multi-page build (`index.html` **+ `viewer.html` — the dev-only WAD viewer currently ships in the production bundle**); `serveWads()` plugin serves `wads/` (gitignored, D005 fetch) over `/wads/` in **dev + preview only** — a static deploy of `dist/` has NO wad: the boot story is `?wad=<url>` → `/wads/freedoom1.wad` (404 → file-picker branch, M1-08) and must be documented, not silently broken. NO PWA, NO service worker, NO manifest (grep empty). `debug.ts` gates `__doom` to dev builds or `?test=1` — the production build must prove `__doom` ABSENT without the flag (M12-06 asserts). No README at repo root; `LICENSE` (GPL-2.0-or-later) + `CREDITS.md` (no-id-assets statement, Freedoom BSD-3 pin pointer) exist and are the audit seed.

0.7. **Browser matrix reality**: playwright projects = `chromium` + `chromium-audio` ONLY. Engine-risk surfaces already identified in code: IndexedDB (fail-closed ladder + memory backend in `persist/idb.ts` — Safari private-mode class), AudioContext gesture policy (`audio/context.ts` autoplay gate), `requestPointerLock` (abstracted seam in `input/mouse.ts:78` — webkit prefix history), `setTargetAtTime` (`audio/volumes.ts` — universal). Plan: CI stays chromium; firefox/safari = manual checklist + OPTIONAL experimental `firefox`/`webkit` projects behind an env flag (D-12e).

0.8. **Demo-compat perimeter**: D-11e already declares demos as first-class BYTES with the vanilla-shape 13B header + 4B/tic body (§M11 plan 0.5); the pinned WAD has zero DEMO lumps; our sim is a faithful-but-independent reimplementation ⇒ cross-tool interop (playing our `.lmp` in vanilla/chocolate or theirs here) is **NOT claimable** and never was — M12 documents the ours-only perimeter (D-12c); format goldens already prove header/body byte-shape.

0.9. **"M12" markers in src (the code's IOUs — all RESOLVED in §3, none becomes netcode)**: `pDemo.ts`/`thinglinks.ts`/`reborn.ts`/`game.ts` "M12 netgame respawn/netgames" comments; `gamemode.ts` "M12 corpus decides" (E2+ reach) + "M12 transcribes" (E2-E4 pars rows); `dehacked.ts` thing-property edits "M12 stretch"; `game.ts` `ga_screenshot` "(M12)" stub; `bsp.ts` "M12 audit"; `menu.ts` devparm-screenshot note.

## 1. Task graph and parallel waves

| Wave | Tasks (parallel) | Depends |
|---|---|---|
| 1 | M12-01, M12-05, M12-06, M12-07 | M11 main |
| 2 | M12-02, M12-03, M12-04 | M12-01 |
| 3 | M12-08, M12-09, (gap-fix follow-ups from 01/02/04) | wave 2 |
| 4 | M12-10 | all |

**Disjoint owns (one owner per path).** M12-01 owns `scripts/map-census.mjs`, `tests/headless/allMapsCensus.test.ts`, `docs/reports/M12-census.md`. M12-02 owns `tests/render/viewpointsAllMaps.ts`, `tests/render/allMaps.test.ts`, goldens set `maps/`, `docs/reports/M12-montage.md` (montage assembly per D016 convention). M12-03 owns `tests/fixtures/m12Routes.ts`, `tests/headless/routes.test.ts`, `e2e/m12-playthrough.spec.ts`. M12-04 owns `tests/perf/**`, `e2e/m12-perf.spec.ts`, `scripts/perf-probe.mjs` (node-side sim-only probe) — render-hot-path EDITS are requested as a patch-list, applied in-wave 3 under a fix-task tag if (and only if) the profile violates budget. M12-05 owns `tests/e2e-soak/**` (extends harness.ts — the header pre-books it). M12-06 owns `vite.config.ts`, `e2e/build.spec.ts`, `playwright.config.ts` (build project). M12-07 owns `scripts/license-audit.mjs`, `CREDITS.md`. M12-08 owns `docs/release/browser-matrix.md` + the experimental browser-project REQUEST (patch-list onto playwright.config.ts, applied after M12-06 merges). M12-09 owns `README.md`, `CONTROLS.md`, `docs/release/*` (play guide). M12-10 owns `docs/**` (+ marker-retag comment edits per D-12d). `src/sim/**` and `src/render/**` are OFF-LIMITS to every wave-1/2 task — findings become follow-up fix tasks with their own tags (this milestone must not silently re-bless anything: a golden re-bless is a FINDING, per M10-10 convention).

## 2. Per-task specifications

### M12-01 — Every-map census live-check (specials, sprites, structure, lumps)
- **Goal**: headless suite loading ALL nine E1 maps from the pinned WAD: (a) structural sanity — subsector/BSP walk terminates, blockmap consistent (reuse A-FX1 property tools), `P_SetupLevel` completes, per-map spawn census (things at WAD positions, per-skill like spawn-skill.spec); (b) SPECIALS coverage on real maps — every used line/sector special from §0.2 exercised (drive the use/cross/shoot routes for the map's used-id SET; assert `unimplementedSpecial.count == 0` + `registryManifest` superset check per map); (c) sprite/doomednum coverage — `rthings.skipped.missingSprite == 0` for all E1 things; (d) lump-presence existence checks for every content surface §0.3 lists as needed (titles via MAPNAMES, M_DOOM, WILV*, INTERPIC, D_* music per map). Any gap = typed finding in the report + follow-up fix task; the census itself edits no src.
- **Owns**: `scripts/map-census.mjs` (pure WAD scan), `tests/headless/allMapsCensus.test.ts`, `docs/reports/M12-census.md`.
- **Verify**: `npx vitest run tests/headless/allMapsCensus`
- **Acceptance**: 9/9 maps green, gap table closed (or itemized follow-ups filed), census JSON committed as evidence.

### M12-02 — Every-map L3 render corpus (≥8 viewpoints × 9 maps) + montage
- **Goal**: `viewpointsAllMaps.ts`: per map ≥8 analytically-derived viewpoints (spawn, each key door vantage, secret areas — derived from the map's sector/line graph via the census data, same no-screenshot derivation discipline as viewpoints.ts M4-08), every frame asserted HOM == 0 + no single-color/anomaly flags (the existing renderer counters), indices-hash goldens committed as set `maps` (≥72 goldens; PNG companions for the montage pack). Montage sheet into `docs/reports/M12-montage.md` for the human-eyes gate (D016).
- **Owns**: as §1. **Must not touch**: existing golden sets (adds only — a drift in an old set is a FINDING).
- **Verify**: `npx vitest run tests/render/allMaps && node scripts/goldens-update.mjs --set maps --check`

### M12-03 — Reachable-exit routes per map + scripted playthroughs
- **Goal**: per map a deterministic scripted route (seeded input commands via the M9 harness) that reaches the exit: L2 sim-level (exit-type reached, `G_ExitLevel` fires, intermission enters stats state) + zero unimplemented-special hits; L4 browser: full E1M1 playthrough + E1M8 boss fight route (sector-special-11 finale math) + one more map (E1M2 or census-picked), each ending at the WI/finale state with zero console errors. Secret-route bonus where a route can reach the secret exit (documented, not gated).
- **Owns**: as §1. **Verify**: `npx vitest run tests/headless/routes && npm run e2e -- m12-playthrough`

### M12-04 — Perf: measure, then pin the frame budget (no premature optimization)
- **Goal**: (a) node-side `perf-probe`: fixed scripted scenes (E1M7 heaviest + E1M1 baseline + a 40-mobj firefight) — sim-tic and render-frame ms, p50/p95, run under `--cpu-prof`; (b) browser-side `m12-perf.spec.ts`: real rAF loop at 320×200, 30 s per scene, `__doom.state().perf` counters (new ADDITIVE debug read, timing platform-side — sim stays pure) asserting the PINNED budget; (c) the budget proposal: p50 ≤ 8 ms (ARCH §4.8), p95 ≤ 13 ms, pinned-after-first-measure (D-12a — measured values go into JOURNAL + DECISIONS verbatim; targets are then frozen, not tuned-to-pass). (d) If violated: profile-guided hot-path fixes land as a tagged wave-3 fix task (renderer column loop / 64k RGBA convert / sprite pass are the §4.8 suspects); if met: zero optimization commits — the profile is the deliverable.
- **Owns**: as §1 (src edits ONLY inside a follow-up fix task tag). **Verify**: `npx vitest run tests/perf && npm run e2e -- m12-perf`

### M12-05 — Soak marathon + memory/pool leak audit (the BUGS.md debt, banked)
- **Goal**: extend `tests/e2e-soak/harness.ts` (parity contract honored — mirror any main.ts drift): seeded PRNG input generator (splitmix over the A-09 key table: move/fire/use/weapon-cycle/automap-toggle/menu-visits at random rates) + level rotation (exit-triggered or idclev-seam) + per-tic invariants (no NaN in hash inputs, hook-log caps, hash self-consistency); DEFAULT run CI-safe ≈ 10 min sim-clocked, `SOAK_MINUTES=30` marathon profile documented. Memory assertions: mobj-arena slot census (occupied-slot count returns to post-setup band after each level + across ≥5 faithful-reborn restarts), thinker-pool census, hook-log caps, and a heap-growth check (v8 heapUsed delta < threshold over 30 min, GC-forced sampling every 60 s) — growth beyond threshold = FINDING with a retained-object profile attached.
- **Owns**: `tests/e2e-soak/**`. **Verify**: `npx vitest run tests/e2e-soak` (+ marathon once, logged in JOURNAL)

### M12-06 — Production build e2e + release-surface hygiene
- **Goal**: (a) `vite.config.ts`: the game build ships `index.html` ONLY — viewer.html moves to a dev-only entry (or a separate `build:viewer` — pick the simpler, document it); (b) `e2e/build.spec.ts`: `vite build` + `vite preview` (new playwright project) — boot → E1M1 playable over the preview server (the serveWads middleware runs there — asserted), `typeof window.__doom === 'undefined'` WITHOUT `?test=1` and present WITH it (no dev-only seam leakage), no `console.error`, bundle-size budget pinned-after-measure with a hard cap (proposal: ≤ 500 KB gzipped JS total — D-12a's measure-then-pin applies here too), dist contents asserted (no `viewer-*.js`, no source maps in the shipped bundle).
- **Owns**: `vite.config.ts`, `e2e/build.spec.ts`, `playwright.config.ts`. **Verify**: `npm run build && npm run e2e -- project=build`

### M12-07 — GPL/license/credits audit (scripted, re-runnable)
- **Goal**: `scripts/license-audit.mjs` wired into `npm run check`: SPDX header on every source/script file (`GPL-2.0-or-later`), ZERO binary blobs committed (wads/ gitignored — assert), no id-derived lump names as string constants beyond protocol identifiers (grep gate with an allowlist: format names like `E1M1`, `S_START` etc. are protocol, not assets — the audit documents that distinction), Freedoom attribution + BSD-3 pin present in CREDITS.md, LICENSE intact, and a **source-offer statement** for the GPL (link/instructions to this repo build — the compliance line a release needs). CREDITS.md gains the release-complete Freedoom credit block + tooling credits.
- **Owns**: `scripts/license-audit.mjs`, `CREDITS.md`, package.json script entry (additive line, merged early). **Verify**: `node scripts/license-audit.mjs` green + `npm run check` green.

### M12-08 — Browser matrix: manual checklist + optional experimental projects
- **Goal**: `docs/release/browser-matrix.md`: per-engine known-risk table (§0.7: IDB private-mode/blocked → fail-closed ladder expectations; autoplay gesture requirement; pointer-lock grant/deny UX + webkit behavior; `setTargetAtTime`; fetch streaming of the 31 MB wad) + a 15-minute manual test script (boot, play, save/reload, audio on/off, fullscreen/pointer-lock) for Firefox + Safari with a findings table. Optionally (decision D-12e): `PW_EXTRA_BROWSERS=1`-gated `firefox`/`webkit` playwright projects running the smoke subset (playstart + one canvas spec) — experimental, non-blocking; webkit audio/IDB quirks documented whatever the smoke says.
- **Owns**: docs + the config REQUEST (applied onto M12-06's playwright.config.ts in-wave). **Verify**: checklist committed; smoke runs at least once, results pasted in.

### M12-09 — Release docs: README play guide + CONTROLS
- **Goal**: root `README.md`: what it is (browser TS port of the 1993 DOOM engine, GPL-2.0+, no id assets), quick start (`npm ci && npm run fetch-freedoom && npm run dev`), build/preview + the SELF-HOST recipe (dist/ + serving a freedoom1.wad at `/wads/…` or using the file picker / `?wad=` — §0.6 truth), browser support (matrix doc link), project layout map. `CONTROLS.md`: the A-09 default table (m_misc.c-cited keys + WASD additions), mouse/pointer-lock, F-keys (F2/F3 menus, F6/F9 quicksave/load — the M11 folklore deaths restated), the full cheat table (from `docs/reports/M11-cheats.md`). `docs/release/notes.md`: tag `v0.12.0` GitHub release notes skeleton (features by milestone, known limitations: E1-only shareware policy, netgame absent, chromium-tested-first, ours-only demos D-12c).
- **Owns**: `README.md`, `CONTROLS.md`, `docs/release/**`.

### M12-10 — Exit sweep + docs + tag
- **Goal**: full gates (`npm run check && npm run e2e && goldens --check` incl. new `maps` set), DECISIONS D-12a..e records + the §0.9 marker-retag (comment-only src edits: "M12" IOUs → "Post-M12 stretch" where D-12d defers them), ROADMAP M12 `[x]` (+ P4 preview note: audits/playtests continue in Phase 4 using THIS milestone's harnesses), JOURNAL perf-log entry (the budget table — ARCH §4.8's promise discharged), TASKS/STATUS, VISUAL-REVIEW montage session scheduled, tag `v0.12.0` + release notes shipped.
- **Owns**: `docs/**` + marker-retag comment edits.

## 3. Deviations, deferred hooks, and open decisions

- **D-12a (M12-04/06) measure-then-pin budgets**: frame budget proposed at p50 ≤ 8 ms / p95 ≤ 13 ms (sim+render, 320×200, mid-range laptop class) and bundle at ≤ 500 KB gzipped — both are PROPOSALS until the first honest measurement; the pinned numbers (measured values + hard caps) land in DECISIONS/JOURNAL at exit, and CI asserts the caps. No optimization commit without a profile showing violation.
- **D-12b (M12-01/02) ship scope = E1-only shareware policy, on a full-IWAD pin**: freedoom1.wad IS the 4-episode IWAD (36 maps, §0.1) and all code paths for E1M1..E1M9 exist; `GAME_MODE=shareware` stays pinned for v0.12.0 (episode-select/menu surface, pars E2-E4, and the E1→E2 boundary stay dormant — transcribe E2+ pars ONLY when E2+ unlocks, Post-M12; §0.9 markers retagged). "One map per episode" from the ROADMAP row resolves to E1M1 + E1M8 + E1M9-finale under this policy.
- **D-12c (M12-03/docs) demo compat is OURS-ONLY, stated publicly**: byte-shape is vanilla-faithful (header/body goldens) but interop is unclaimable (independent sim, D-11e bytes, zero DEMO lumps); the release notes state the perimeter instead of leaving it as folklore.
- **D-12d (§0.9 netcode markers)**: NO netgame lands in M12 — there is no networking milestone and 1.10 netcode is out of scope; the "M12 netgames/netgame respawn" comments in pDemo/thinglinks/reborn/game, the `ga_screenshot` stub, dehacked thing-edits, and `bsp.ts`'s audit note are retagged "Post-M12 stretch" (documentation truth, not a task). The screenshot seam may land as a download affordance Post-M12 (M11 §3 precedent).
- **D-12e (M12-08) CI stays chromium**: multi-engine CI triples wall-time for near-zero signal until someone reports an engine bug; the manual matrix + env-gated experimental projects are the compliance surface. Revisit at P4 if the matrix reports a finding.
- **D-12f (M12-06) NO service worker / PWA**: offline means caching a 31 MB user-supplied wad we do not ship; the honest offline story is the file-picker + `?wad=` path (works from `file://`-adjacent static hosting too), documented in README. Revisit only if a host wants an app-shell install flow.
- **Deferred / carried (counted, none new-console)**: M10 ear-session + M11 hands-session human sign-offs → scheduled inside M12-10's montage/exit pack (they are checklists awaiting sessions, not code); VISUAL-REVIEW contrast fidelity-watch + weapons re-review → the M12-02 montage pack absorbs both (real-map views supersede fixture views); chat sfx, bind-menu UI, netgame save semantics, user-demo attract, widescreen, Doom II, mouselook, GM soundfont → Post-M12/P4 stretch lists (ROADMAP).

## 4. Exit criteria (milestone-level, objectively checkable)

**ROADMAP M12 row, QUOTED verbatim**: "Every Freedoom P1 map loads + renders at ≥8 sampled viewpoints with HOM=0 and no single-color/anomaly flags (L3 corpus); reachable exit per map (L2/L4 route); scripted playthrough of E1M1 + one map per episode (L4); perf log: 35 Hz sim + 60 fps on mid-range laptop". Decomposed (episode clause per D-12b):

1. **Census**: 9/9 E1 maps load headless; specials/sprites/structure/lump census green or itemized-then-fixed (`docs/reports/M12-census.md`); zero `unimplementedSpecial` hits on real maps.
2. **Render corpus**: ≥72 goldens (9 maps × ≥8 viewpoints incl. secret areas), all with HOM==0 + anomaly flags clear, `maps` set `--check` drift-free; montage human-reviewed (D016 sign-off in VISUAL-REVIEW).
3. **Routes/playthrough**: per-map L2 scripted exit route green; L4 browser playthroughs: E1M1 + E1M8 + E1M9-finale (D-12b reading of "one per episode") with zero console errors.
4. **Perf log**: measured table (p50/p95 per scene, sim vs render split) in JOURNAL; pinned budget green in `m12-perf.spec.ts` (or the tagged fix wave landed and then went green).
5. **Soak**: default soak suite green in CI; 30-min marathon run once, memory-growth assertions pass, arena census returns to band across ≥5 faithful reborns; results in JOURNAL.
6. **Release**: `npm run build` + preview e2e green incl. no-`__doom`-without-flag + bundle cap; license-audit script green in `check`; README/CONTROLS/release-notes committed; browser matrix documented (manual pass ≥ Safari + Firefox, findings recorded); tag `v0.12.0`.
7. **Zero-regression + gates**: no existing golden re-blessed without a FINDING entry; `npm run check && npm run e2e` (×2) + goldens `--check` (all sets incl. `maps`) green on integrated main; M10/M11 blessed hashes byte-equal.
8. **Docs**: ROADMAP M12 `[x]` + P4 preview, DECISIONS D-12a..f, marker retags, JOURNAL/TASKS/STATUS per M9-13 convention.

## 5. Wave table (plan of record)

| Wave | Tasks | One-liner |
|---|---|---|
| 1 | M12-01 M12-05 M12-06 M12-07 | Every-map census live-check; soak marathon + leak audit; production-build e2e + hygiene; license audit script |
| 2 | M12-02 M12-03 M12-04 | All-maps L3 viewpoint corpus + montage; reachable-exit routes + playthroughs; perf measure-then-pin |
| 3 | M12-08 M12-09 (+fix follow-ups) | Browser matrix + experimental projects; release docs; any census/profile-driven fixes |
| 4 | M12-10 | Exit sweep + docs + tag v0.12.0 |
