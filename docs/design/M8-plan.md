# M8 Plan — "monsters hunt you": state-table AI, sight/sound wake, melee/missile, pain/death/gib, infighting

Status: planned. Parent: docs/ROADMAP.md M8 (+ the A-02 DEHACKED fullbright cross-cutting task, which lives here as M8-12). Inputs: ARCHITECTURE.md §2.3 (mobj/state tables), §3.2/§3.5 (tic order, thinker arena, `P_RunThinkers` rules), §3.3/§5.2 (single P_Random stream + call-site ledger), §5.4 (monster-AI zone), R07 (primary monster-AI note), R06 (state/mobj semantics, P_KillMobj, P_SpawnMapThing), R08 (weapon/hitscan machinery M8 reuses verbatim: P_AimLineAttack/P_LineAttack/P_SpawnMissile/P_RadiusAttack), R05 (monster-gated specials + MLsoundblock/ML_BLOCKMONSTERS), R04 (P_CheckSight/P_TryMove/REJECT), R12 (Freedoom parity), R02 §2 (sprites/fullbright for the DEHACKED task). Predecessors: M5-plan (pmove/pmap/pmaputl), M6-plan (specials + thinker arena + hook slots), M7-plan (**the direct predecessor** — tables, mobj runtime, hitscan/projectiles, damage-slot convention, random-sites ledger).

Exit (from ROADMAP): full Phase-1 roster from the state tables; wake on sight/sound; melee + missile attacks; pain/death/gib; infighting; exploding barrels — L2 per-family fixture suites + random-site counts; L5 death-state screenshot review.

## 0. Source truths pinned this pass

0.0. **Source tree verification: the local mirror is COMPLETE this pass.** `/tmp/DOOM-master/linuxdoom-1.10/` holds **62 `.c` files** (`ls *.c | wc -l` == 62, verified this pass), i.e. unlike the M6-§0.1 / M7-§0.1 situation ("only r_*/g_game.c on disk"), every file cited below was read from the mirror itself: `p_enemy.c` (2008 lines), `p_sight.c`, `p_inter.c`, `p_mobj.c`, `p_map.c`, `info.c`. Research note R07 remains the reasoning record; **the raw file is the arbiter**, and D015 (mirror = untrusted data, checksum-pin before trusting) still applies to any line quoted into code comments. No re-download was needed.
0.1. **p_enemy.c function census (2008 lines, with line cites).** AI core: `P_RecursiveSound` :106 · `P_NoiseAlert` :159 · `P_CheckMeleeRange` :174 · `P_CheckMissileRange` :197 · `P_Move` :272 (+`xspeed/yspeed` LUT :264) · `P_TryWalk` :349 · `P_NewChaseDir` :363 (+`opposite[]`/`diags[]` LUT :70/77) · `P_LookForPlayers` :499. Action routines: `A_KeenDie` :566 · `A_Look` :604 · `A_Chase` :672 · `A_FaceTarget` :782 · `A_PosAttack` :802 · `A_SPosAttack` :821 · `A_CPosAttack` :845 · `A_CPosRefire` :865 · `A_SpidRefire` :882 · `A_BspiAttack` :898 · `A_TroopAttack` :913 · `A_SargAttack` :935 · `A_HeadAttack` :950 · `A_CyberAttack` :969 · `A_BruisAttack` :979 · `A_SkelMissile` :1002 · `A_Tracer` :1021 · `A_SkelWhoosh` :1093 · `A_SkelFist` :1101 · `PIT_VileCheck` :1129 · `A_VileChase/Start/Target/Attack` :1167/1233/1285/1310 (+`A_StartFire` :1245, `A_FireCrackle` :1251, `A_Fire` :1257) · `A_FatRaise` :1351, `A_FatAttack1/2/3` :1358/1375/1392 · `A_SkullAttack` :1419 · `A_PainAttack` :1512 · `A_PainDie` :1522 · `A_Scream` :1535 · `A_XScream` :1572 · `A_Pain` :1577 · `A_Fall` :1585 · `A_Explode` :1598 (→`P_RadiusAttack(spot, thing->target, 128)` — M7-09 already lives) · `A_BossDeath` :1609 · `A_Hoof/A_Metal/A_BabyMetal` :1759/1765/1771 · brain family `A_BrainAwake…A_SpawnFly` :1813-1936 · `A_PlayerScream` :1994 (p_mobj.c:1044 A_PlayerScream is the *mobj* version; the player-death states call the p_enemy one — pin which one the S_PLAY_DIE rows reference at impl). **Out of E1 scope by roster (§0.6): the VILE, FATSO, SKEL, PAIN, BRAIN/SPID-CYBER-boss and KEEN families — every one of those functions is a registered-but-counted stub this milestone (§4).**
0.2. **Wake rules — `P_LookForPlayers` (:499) + `A_Look` (:604).** `P_LookForPlayers`: `sector = actor->subsector->sector`; `stop = (lastlook-1)&3`; infinite loop advancing `lastlook = (lastlook+1)&3`; `continue` on `!playeringame[lastlook]`; **bail `false` when `c++ == 2 || lastlook == stop`** (so at most two live players are examined per call — a 4-player ring that never scans all four); skip `player->health <= 0`; `P_CheckSight` or continue; `allaround==false` ⇒ the 180° test (`an = R_PointToAngle2(…) - actor->angle; if (an > ANG90 && an < ANG270)` ⇒ skip **unless** `dist <= MELEERANGE`, i.e. "react anyway if really close"); success sets `actor->target = player->mo` (no sound, no threshold change). `A_Look` (runs every tic of the STND states — 33 A_Look rows): `threshold = 0` **unconditionally every tic** ("any shot will wake up"); `targ = sector->soundtarget` — if it exists and is `MF_SHOOTABLE` ⇒ `target = targ`, and `MF_AMBUSH` adds a `P_CheckSight` gate, else straight to `seeyou`; otherwise `P_LookForPlayers(actor,false)`; on success the see-sound plays — `sfx_posit1/2/3 → sfx_posit1+P_Random()%3`, `sfx_bgsit1/2 → +P_Random()%2`, default = `info->seesound` — with `MT_SPIDER/MT_CYBORG` using `S_StartSound(NULL,…)` (full volume, no attenuation origin) — then `P_SetMobjState(seestate)`. **`lastlook` is seeded `P_Random() % MAXPLAYERS` at spawn (p_mobj.c:508), already implemented (`p_mobj.ts` `lastLook`) — the wake order is therefore PRNG-coupled at load time, not just at runtime.**
0.3. **Sound wake site (surprise).** `P_NoiseAlert(target, emmiter)` (:159) is called from **exactly one site in the whole tree: `p_pspr.c:256`, inside `P_FireWeapon`** — the M7 stub slot `psprHooks.noiseAlert` (`p_pspr.ts:494/687`) counts it today. `P_RecursiveSound` (:106) floods `sec->lines[0..linecount)` (**P_GroupLines order = ascending linedef index — the M6-§0.4 tie-order rule**) over `ML_TWOSIDED` lines with `P_LineOpening` ⇒ `openrange > 0` (a closed door blocks), sets `sec->soundtarget = soundtarget` and `sec->soundtraversed = soundblocks+1` with the `validcount` domain, and `ML_SOUNDBLOCK` lines recurse **once** with `soundblocks=1` (a sound-blocked hop, not a wall). `A_Look` is the only reader. **Monsters dying/seeing do NOT noise-alert in 1.10 — no other call sites exist** (grep: `p_enemy.c:98/154/159`, `p_pspr.c:256`); monster-to-monster alerting happens through the *damage* path (§0.8), not sound. Sector SoA needs a `soundtarget` field — `map.ts:199` has `soundTraversed` but **no `soundtarget`** (M8-05 owns it).
0.4. **Aggro/attack machine — `A_Chase` (:672), the tic body of 122 state rows.** Order is exact: (1) `reactiontime--` if nonzero; (2) `threshold` decays **only if nonzero**, and is zeroed when `!target || target->health <= 0`; (3) if `movedir < 8`: `angle &= (7<<29)` then turn ±`ANG90/2` toward `movedir<<29` (the classic 45°-quantised wobble); (4) no target **or target not `MF_SHOOTABLE`** ⇒ `P_LookForPlayers(allaround=true)` and return, else **`P_SetMobjState(spawnstate)`** — a lost target returns a monster to its unperturbed stand still (and `P_LookForPlayers` re-seeds nothing); (5) `MF_JUSTATTACKED` set ⇒ clear it, `P_NewChaseDir(actor)` (skipped on nightmare/`fastparm`) and **return** — "do not attack twice in a row"; (6) melee: `info->meleestate && P_CheckMeleeRange` ⇒ `S_StartSound(attacksound)` + `SetMobjState(meleestate)`; (7) missile: if `info->missilestate` — gate `gameskill < sk_nightmare && !fastparm && actor->movecount` ⇒ **no missile while movecount ≠ 0**, else `P_CheckMissileRange` ⇒ state + `flags |= MF_JUSTATTACKED`; (8) `nomissile:` netgame-only retarget when `!threshold && !P_CheckSight`; (9) **chase: `if (--movecount < 0 || !P_Move(actor)) P_NewChaseDir(actor)`**; (10) `activesound && P_Random() < 3` ⇒ play it. Note the flag is **`MF_JUSTATTACKED`** (0x200000 per p_mobj.h — the brief's "MF_JUSTATTACK" doesn't exist), and there is **no `MF_JUSTHIT` consumption in A_Chase** — `MF_JUSTHIT` is read/cleared only by `P_CheckMissileRange` (§0.7).
0.5. **Chase movement: `P_Move` (:272) / `P_TryWalk` (:349) / `P_NewChaseDir` (:363).** `P_Move`: `DI_NODIR` ⇒ false; `movedir ≥ 8` ⇒ `I_Error`; `tryx/y = x/y + info->speed * xspeed/yspeed[movedir]` (**LUT has 47000 for diagonals — diagonal speed is 0.716·speed, and vanilla multiplies fixed×fixed via `FixedMul` implicitly? No: `info->speed*xspeed[dir]` is raw integer product — speed is fixed (8<<16) and xspeed fixed ⇒ the product IS the fixed-scaled delta; port with FixedMul to stay in the same domain — pin the exact expression with an L1 vector test**); `P_TryMove` fails ⇒ if `MF_FLOAT && floatok` adjust `z ±FLOATSPEED` (=4·FRACUNIT, p_local.h:30), set `MF_INFLOAT`, **return true**; else if `!numspechit` return false, else `movedir = DI_NODIR` and run `P_UseSpecialLine(actor, ld, 0)` over every spechit line — **monsters open doors by walking into them** (M6-03's cross/use dispatcher monster gates: `ML_BLOCKMONSTERS` + specials {4,10,88,39,97,125,126}); success clears `MF_INFLOAT` and, for non-floaters, **`z = floorz`**. `P_TryWalk` = `P_Move` then **`movecount = P_Random()&15`** on success (1 draw). `P_NewChaseDir`: `turnaround = opposite[olddir]`; `d[1]` from `deltax` and `d[2]` from `deltay` with a ±10·FRACUNIT dead band; diagonal first via `diags[((deltay<0)<<1)+(deltax>0)]` (skip turnaround) → `P_TryWalk`; then **`if (P_Random() > 200 || abs(deltay)>abs(deltax))` swap d[1]/d[2] — the draw ALWAYS happens** (`||` short-circuits on the left operand); then d[1], d[2] (turnaround nulled), then `olddir`, then **`P_Random()&1` picks clockwise vs counter-clockwise full 8-dir scan** (skipping turnaround), then turnaround, else `DI_NODIR`. **PRNG stream coupling: one successful chase step = 1-2+ draws per monster per tic** (`movecount` reload per success + the swap/search draws), which is the M8 §6-R1 risk: the E1M1 corpus has 1280 killable things (§0.6), so a single wandering imp reorders every downstream weapon/other-monster draw.
0.6. **Range tests.** `P_CheckMeleeRange` (:174): `!target` ⇒ false; `dist = P_AproxDistance(…)`; `dist >= MELEERANGE - 20*FRACUNIT + pl->radius` ⇒ false (**per-victim radius: `MELEERANGE` = 64·FRACUNIT, p_local.h:57**); then `P_CheckSight` — so **a melee attempt burns a sight trace**; no PRNG. `P_CheckMissileRange` (:197): `P_CheckSight` first (no LOS ⇒ **no draw**); `MF_JUSTHIT` ⇒ **clear the flag and return true** ("the target just hit the enemy, so fight back!"); `reactiontime` ⇒ false; `dist = P_AproxDistance − 64*FRACUNIT`, `−128*FRACUNIT` if no `meleestate`, `dist >>= 16` (now map units), type tweaks — `MT_VILE > 14*64` reject, `MT_UNDEAD < 196` reject then `>>=1`, `MT_CYBORG|MT_SPIDER|MT_SKULL → >>=1`, clamp `>200 → 200`, `MT_CYBORG && >160 → 160`; then **`if (P_Random() < dist) return false`** — exactly **one draw** whenever a monster has LOS and a missile state. This is the second big stream consumer.
0.7. **Attack bodies (E1 subset; from `info.c` rows + p_enemy.c bodies).**
   - `A_PosAttack` (:802, `S_POSS_ATK2`): `A_FaceTarget` → `P_AimLineAttack(angle, MISSILERANGE)` → `sfx_pistol` → `angle += (P_Random()-P_Random())<<20` → `damage = ((P_Random()%5)+1)*3` (3/6/9/12/15) → `P_LineAttack(MISSILERANGE, slope, damage)`. **The "possessed" shoot like a pistol: hitscan, no projectile mobj.**
   - `A_SPosAttack` (:821, `S_SPOS_ATK2` and **also `S_SPID_ATK2/3`** — the Spider Mastermind reuses it): `sfx_shotgn`, then **3 iterations** of `angle = bangle + ((P_Random()-P_Random())<<20); damage = ((P_Random()%5)+1)*3; P_LineAttack(…)` ⇒ **9 draws** (2 jitter + 1 damage each).
   - `A_CPosAttack`/`A_CPosRefire` (:845/:865, CPOS+SSWV rows, 4+2 rows): one shot per refire tic with `P_Random() < 40` "keep firing, do nothing" gate, back to `seestate` when the target dies/leaves sight. **No CHAINGUY (65) in E1 (§0.6) — registered but unreachable, fixture-only.**
   - `A_TroopAttack` (:913, single `S_TROO_ATK3` row shared by melee+missile states): `A_FaceTarget`; if `P_CheckMeleeRange` ⇒ `sfx_claw` + `(P_Random()%8+1)*3` (3..24) via `P_DamageMobj(target, actor, actor, d)`; **else `P_SpawnMissile(actor, target, MT_TROOPSHOT)`**.
   - `A_SargAttack` (:935, `S_SARG_ATK3`; SPECTRE shares the rows): melee only, `damage = ((P_Random()%10)+1)*4` (4..40), **no attacksound here** (`sfx_sgtatk` fires from `A_Chase`'s melee branch).
   - `A_HeadAttack` (:950, `S_HEAD_ATK3`): melee `(P_Random()%6+1)*10` (10..60) else `MT_HEADSHOT`. **No CACO (3005) in E1 (§0.6).**
   - `A_BruisAttack` (:979, `S_BOSS_ATK3` = MT_BRUISER/baron and `S_BOS2_ATK3` = MT_KNIGHT): melee `sfx_claw` + `(P_Random()%8+1)*10` (10..80) else `MT_BRUISERSHOT`.
   - `A_CyberAttack` (:969, 3 rows): `A_FaceTarget` + `P_SpawnMissile(…, MT_ROCKET)` — **the cyberdemon fires the player's own rocket mobj.**
   - `A_BspiAttack`/`A_SkelMissile`/`A_FatAttack*`/`A_PainAttack`/`A_Vile*`/`A_SpidRefire`: Doom-2-only sources (64/66/67/68/71) ⇒ **stub-only (§4)**, no E1 exposure.
   - `A_SkullAttack` (:1419, `S_SKULL_ATK2`): `flags |= MF_SKULLFLY`, `attacksound`, face, `mom = SKULLSPEED·dir`, `momz = (dest.z + dest->height/2 − actor->z)/dist`; the damage lands in `PIT_CheckThing`'s skullfly branch: `damage = ((P_Random()%8)+1)*info->damage` (MT_SKULL damage 3 ⇒ 3..24) then flag cleared, momentum zeroed, `SetMobjState(spawnstate)`.
   - `A_FaceTarget` (:782): clears `MF_AMBUSH`, aims, and **`if (target->flags & MF_SHADOW) angle += (P_Random()-P_Random())<<21`** — i.e. **2 draws per face** when the *target* is fuzzy (a spectre attacking a spectre, or an invisible player).
0.8. **Damage/kill, pain, infighting, gib (p_inter.c:775 / :668).** `P_DamageMobj`: bail on `!MF_SHOOTABLE` or `health<=0`; `MF_SKULLFLY` ⇒ momentum zeroed; player+baby ⇒ `damage >>= 1`; **thrust** (any inflictor, non-chainsaw source): `ang = R_PointToAngle2(inflictor→target)`, `thrust = damage*(FRACUNIT>>3)*100/mass`, and the "fall forwards" case `damage<40 && damage>health && target.z − inflictor.z > 64*FRACUNIT && (P_Random()&1)` ⇒ `ang += ANG180; thrust *= 4` — **1 draw, and only when the earlier `&&`s pass** (order matters for the stream!); player branch (god/invul `damage<1000` bail, armor /3 |/2, `health` mirror, `attacker = source`, `damagecount` clamp 100, `I_Tactile`); `health -= damage` → `<=0 ⇒ P_KillMobj(source,target)` and return; **else `if (P_Random() < info->painchance && !(flags&MF_SKULLFLY))` ⇒ `flags |= MF_JUSTHIT` + `SetMobjState(painstate)`** (1 draw always taken on a non-fatal hit); `reactiontime = 0`; and the **retarget/infighting rule**: `if ((!target->threshold || type==MT_VILE) && source && source != target && source->type != MT_VILE)` ⇒ `target->target = source; target->threshold = BASETHRESHOLD (=100, p_local.h:61); if (state == spawnstate && seestate) SetMobjState(seestate)`. **That is the whole infighting implementation** — a monster hit by another monster (or its missile, `source = missile->target`) switches to the attacker while `threshold == 0`; players keep `threshold` decaying in `A_Chase`.
   `P_KillMobj` (:668): clear `MF_SHOOTABLE|MF_FLOAT|MF_SKULLFLY`; clear `MF_NOGRAVITY` **except** `MT_SKULL`; set `MF_CORPSE|MF_DROPOFF`; **`height >>= 2`** (the corpse is a quarter as tall for the rest of its life — every later sight/hit test sees that); `killcount++` for `MF_COUNTKILL` when the source is a player, and `players[0].killcount++` for **any** monster death when `!netgame` ("even those caused by other monsters"); player branch (frags, `PST_DEAD`, `P_DropWeapon`, AM_Stop); then **the gib rule: `if (health < -spawnhealth && xdeathstate) SetMobjState(xdeathstate) else SetMobjState(deathstate)`, then `tics -= P_Random()&3; if (tics < 1) tics = 1`** — **1 draw per death** (no separate gib-debris mobjs: `MT_GIBS`/`MT_GOREBODY` **do not exist in 1.10** — grep-verified §0.9); finally the drop table (POSS→MT_CLIP, SHOTGUY→MT_SHOTGUN, CHAINGUY→MT_CHAINGUN, `MF_DROPPED`), which the TS already implements verbatim in `pplayer.ts`.
   **Blood is spawned from exactly one site — `PTR_ShootTraverse` (`p_map.c:1008 P_SpawnBlood`)**: melee, missile and radius damage spawn **no blood at all** in 1.10 (grep: `P_SpawnBlood` callers = p_map.c only), and `MT_BARREL` is `MF_NOBLOOD`.
0.9. **Corpse lifecycle & monster movement/collision truths.** `A_Fall` (:1585) is the corpse's *only* bookkeeping action: **`flags &= ~MF_SOLID`** — "actor is on ground, it can be walked over"; it is the action of 23 state rows (e.g. `S_SPOS_DIE3`, `S_TROO_DIE4`, `S_BOSS_DIE5`), so the death animation's *early* rows still carry `MF_SOLID` and **block monsters/the player until the A_Fall tic**, after which `PIT_CheckThing` returns `!(MF_SOLID)` = pass (and `PIT_StompThing` p_map.c:81 `!tmthing->player && gamemap != 30 ⇒ return false` still refuses to let a monster *stomp* anything, shootable or not). `MF_CORPSE`'s other consumer is **`P_XYMovement` friction (p_mobj.c:207): a corpse with `|mom| > FRACUNIT/4` and `floorz != subsector->sector->floorheight` skips friction entirely** (drifts off ledges). `P_KillMobj`'s `height >>= 2` + `MF_DROPOFF` also change what later monsters see/shoot over — the "corpse effect on movement" exit criterion is exactly this trio (A_Fall timing, stomp rule, corpse friction).
   **Mutual monster "push": there is none in 1.10.** `PIT_CheckThing` (p_map.c:252) is the whole story — no mass rules, no `P_RecursiveSound`-style shove, no `PIT_TryMoveStack`: a solid thing simply returns false and `P_Move` fails ⇒ `P_NewChaseDir` routes around it (M5-03's `pitCheckThing` already implements exactly this; the M8 additions are the **skullfly branch** (:276, currently `pmapHooks.skullFlyHit` counter) and the **missile same-species rule** (:301, `missileThingCheck` hook, incl. the `MT_KNIGHT↔MT_BRUISER` alias pair), plus `P_ThingHeightClip` (p_map.c:518-578) for monsters riding moving floors — M6's movers already call it? If not, that's a M8-05 discovery item.
   **Gravity/float:** `P_ZMovement` (p_mobj.c:246): `MF_FLOAT && target` (not SKULLFLY, not INFLOAT) hovers toward `target.z + height/2` by `FLOATSPEED` when `dist < 3·|delta|`; floor hit zeroes `momz` (`MF_MISSILE` ⇒ explode), **`else if (!(MF_NOGRAVITY))`** applies `-GRAVITY*2`/`-GRAVITY`; ceiling hit zeroes upward `momz`. Floaters (`MT_HEAD` = FLOAT|NOGRAVITY, `MT_SKULL` = FLOAT|NOGRAVITY) never `z = floorz` in `P_Move` (§0.5) and land only via the hover rule — **no "landing" state, no A_CPosDie-style action: corpse states run at their z** (the brief's "MF_FLOAT landing rules" = these two blocks and nothing else).
0.10. **Sight: `p_sight.c` is its own file (20 .c files exist; p_sight.c is real).** `P_CheckSight(t1,t2)` (:300): REJECT bit test with `s = (t1->subsector->sector - sectors)` (`sightcounts[0]++` on trivial reject); `validcount++`; **`sightzstart = t1->z + t1->height − (t1->height>>2)`** (¾ height — caco 56 ⇒ z+42, player ⇒ z+42), `topslope = (t2->z + t2->height) − sightzstart`, `bottomslope = t2->z − sightzstart`; `strace` = t1→t2 then `P_CrossBSPNode(numnodes-1)` (**descending node index, head = last node**). `P_CrossSubsector` (:126): `validcount` line dedup, `P_DivlineSide` (returns 0/1/2 with the axis-aligned special cases :55), non-crossing skips, **`!(ML_TWOSIDED) ⇒ return false`** (a one-sided line always stops the trace), `front/back` equal floor+ceiling ⇒ `continue`, `opentop = min(ceilings)`, `openbottom = max(floors)`, `openbottom >= opentop ⇒ false`, then slope narrowing from `FixedDiv(openbottom − sightzstart, frac)` / `(opentop − …)` and `topslope <= bottomslope ⇒ false`. **No PRNG, no MF_SHADOW/stealth test, no range limit** — an unobstructed LOS is infinite (only geometry/reject stop it), and monsters see invisible players fine (`MF_SHADOW` only jitters *their* aim, §0.7). The TS already has a working `pCheckSight` — **in `pradius.ts:117`** (M7-09's splash-LOS), implemented on `pPathTraverse` + `sectorAtPoint` for the reject indices rather than the dedicated BSP walk; M8-02 owns the move to `psight.ts` with (a) `subsector→sector` indices for REJECT, (b) the `ML_TWOSIDED` early-false order, and (c) `sightcounts[2]` exposed; every consumer (`P_CheckSight` in A_Look/A_Chase/P_CheckMeleeRange/P_CheckMissileRange/PIT_RadiusAttack) then shares one implementation.
0.11. **Monster state-table census (info.c rows; the actions M8 must register).** `A_Chase` **122 rows** · `A_FaceTarget` 43 · `A_Look` 33 · `A_Fall` 23 · `A_Pain` 19 · `A_Scream` 19 · `A_XScream` 6 · `A_BossDeath` 5 · `A_SpidRefire` 2 · `A_CPosAttack` 4 / `A_CPosRefire` 2 · `A_CyberAttack` 3 · `A_SPosAttack` 3 · `A_BspiAttack` 3 · `A_Metal` 4 / `A_BabyMetal` 2 / `A_Hoof` 1 / `A_Tracer` 2 · `A_Explode` 2 (M7-09 has it) · `A_BrainDie/…Spit/…` 6 · `A_Vile*` 12+ · `A_KeenDie` 1 · `A_PainDie` 1 · `A_PlayerScream` 1. Per-family row counts (E1 roster): POSS 33 (2 A_Look, 8 A_Chase, 1 A_FaceTarget, 1 A_PosAttack, 1 A_Pain, 1 A_Scream, 1 A_XScream, 2 A_Fall, 16 NULL), SPOS 34 (A_SPosAttack ×1, fullbright frame `32773` on ATK2), TROO 33 (A_TroopAttack ×1 on `S_TROO_ATK3`, ATK1/2 are A_FaceTarget), SARG 27 (**no XScream/xdeath: `xdeathstate = S_NULL`**), HEAD 20 (single A_Chase row — the caco has 1-tic? no: `S_HEAD_STND_CENT`? verify at impl: HEAD has 1 A_Chase + 1 A_Look), BOSS 29 (`A_BossDeath` on `S_BOSS_DIE7`), BOS2 29 (same shape, no BossDeath), SKULL 16 (A_Look ×2, A_Chase ×2, `A_SkullAttack` on ATK2). Death-state shape everywhere: `DIE1..DIE5` with `A_Scream` on the second row and `A_Fall` mid-chain, last row `tics = -1 → S_NULL`; XDIE chains 5 tics/row with `A_XScream` on the second.
   mobjinfo facts used by M8 (info.c cites): POSS 3004 hp20 painchance **200** seesound `posit1`(randomized) speed 8 r20 h56 mass100 flags SOLID|SHOOTABLE|COUNTKILL, xdeath S_POSS_XDIE1 · SPOS 9 hp30 pc **170** · TROO 3001 hp60 pc 200 meleestate=missilestate=`S_TROO_ATK1` · SARG 3002 hp150 pc 180 speed **10** r30 mass **400** melee-only · SHADOWS 58 = SARG rows + **MF_SHADOW** (doomednum **58**) · HEAD 3005 hp400 pc **128** FLOAT|NOGRAVITY r31 · BRUISER 3003 hp1000 pc **50** melee+missile `S_BOSS_ATK1` · KNIGHT 69 hp500 pc 50 (Doom2 baron; **not in E1**) · SKULL 3006 hp100 pc **256** damage 3 FLOAT|NOGRAVITY mass 50 **no MF_COUNTKILL** (lost souls never count toward killcount!) · CYBORG 16 hp4000 pc 20 · SPIDER 7 hp3000 pc 40 · BARREL 2035 hp20 flags SOLID|SHOOTABLE|**MF_NOBLOOD**. All of these are already transcribed in `src/wad/info/mobjinfo.ts` (M7-01) — M8 consumes, never re-transcribes.
0.12. **Which doomednums actually appear in E1M1–E1M9 (census; measured this pass on the pinned freedoom1.wad with the linuxdoom-1.10 doomednum→MT table, 10-byte THINGS records).**
   | doomednum | MT | present | alive at skill 3 (single-player bits, no MTF_NOTSINGLE) | where |
   |---|---|---|---|---|
   | 3004 | MT_POSSESSED | 340 | 264 | every E1 map |
   | 9 | MT_SHOTGUY | 380 | 379 | every E1 map |
   | 3001 | MT_TROOP (imp) | 438 | 424 | every E1 map |
   | 3002 | MT_SERGEANT | 166 | 145 | every E1 map |
   | 58 | MT_SHADOWS | 99 | 97 | E1M1,3-7,9 |
   | 3006 | MT_SKULL (lost soul) | 11 | 11 | E1M6 (3), E1M7 (8) |
   | 3003 | MT_BRUISER (baron) | 4 | 4 | **E1M8 only** |
   | 2035 | MT_BARREL | 96 | — (not a monster) | 8 of 9 maps |
   | 3005 / 69 / 65 / 16 / 7 / 71 / 64 / 66 / 67 / 68 / 72 / 84 | HEAD/KNIGHT/CHAINGUY/CYBORG/SPIDER/PAIN/VILE/CYBER?/FATSO/BABY/KEEN/SSWV | **0 in E1M1–E1M9** | — | first appear E2M1+ (HEAD), E2M8 (CYBORG), E3M8 (SPIDER); the Doom-2-only types never appear in Phase 1 |
   Consequences: **(a)** the E1-critical families are POSS/SPOS/TROO/SARG/SPECTRE/SKULL + BRUISER (E1M8) + BARREL; **(b)** MT_SKULL is `!MF_COUNTKILL`, so `killcount`/`totalkills` exclude lost souls by design; **(c)** A_BossDeath's `MT_BRUISER && gameepisode==1 && gamemap==8` gate is live for E1M8 (`junk.tag = 666; EV_DoFloor(lowerFloorToLowest)` — M6's floor engine, an E1M8 exit path M9 will lean on); **(d)** cacos/spectre-fuzz/cyber/spider/vile/pain-elemental code ships **fixture-only** in M8. The committed `monster-census.test.ts` re-derives this table from the WAD at test time (per-map × per-doomednum counts + skill-bit filter + the E1/E2 "absent here" assertions) so no number above is folklore.
0.13. **Hand-offs owed by M7 (verified in-tree this pass).**
   - **`killNonPlayer` counter semantics** (`pplayer.ts:155/427`): today `pKillMobjPlayer` runs the *shared* P_KillMobj body for non-player targets (flag bookkeeping, `height>>2`, killcount bookkeeping, drop table) but counts instead of selecting death/pain states, so **monsters die invisibly** (health ≤ 0, still drawn, still thinkable, `MF_SOLID` retained!). M8-04 takes ownership of the non-player branch: same order, real `xdeath/death` selection + the `tics -= P_Random()&3` clamp; the counter stays as a **must-be-zero-after** guard (like M7's `unimplementedSpecial` idiom).
   - **`pKillMobjShell`** (`p_mobj.ts:734`, `counts.killShellCalls`) — the M5-era shell every non-player call site used; M8-04 retires the shell body (keep the counter field for the M5/M7 tests that read it, or migrate them — the census of readers is M8-04's first step).
   - **P_DamageMobj does not exist yet.** Damage currently terminates in `hooks.damageSlot(h, thingSlot, amount, sourceSlot, tic)` (7 call sites: `p_shoot.ts:390`, `pmap.ts:786` crush, `pmissiles.ts:231`, `pradius.ts:203/289`, `pspec.ts:630/640` hazard floors, `ptelept.ts:366` telefrag) with **slot ids, not mobj refs**. M8-04 owns the bridge: `hooks` gains a registrable `damageBridge(thingSlot, amount, sourceSlot, tic)` that resolves slots→Mobjs through the mobj runtime and keeps recording into `hooks.damage` (the L2 event log stays valid); **no call-site churn in the six files above** (disjointness rule).
   - **Monster pain states deferred**: `A_Pain`/`A_Scream`/`A_XScream`/`A_Fall` (action ids 25/33/28/27) exist in the ActionId registry but are unregistered ⇒ pain/death chains currently stall at the first action row. M8-06 registers them.
   - **M7-09 missile reuse**: `pmissiles.pSpawnMissile(rt, source, dest, type)` is verbatim (spawn `z = source.z + 4*8*FRACUNIT`, `th->target = source`, MF_SHADOW jitter `<<20`, `momz = (dest.z − source.z)/dist` with `dist<1⇒1`, `P_CheckMissileSpawn`) — monster fire bodies call it directly; MT_TROOPSHOT/HEADSHOT/BRUISERSHOT/ARACHPLAZ/ROCKET are already in the tables. `pSpawnPlayerMissile` stays player-only.
   - **`psprHooks.noiseAlert`** counter (`p_pspr.ts:494/687`) → M8-05 fills the body (single call site §0.3).
   - **`pmapHooks` slots for monsters**: `skullFlyHit` (M8-09 skull), `missileThingCheck` (already half-live from M7-09 — needs the MT_KNIGHT↔BRUISER alias), `touchSpecialThing` (done M7-04).
   - **Random-site ledger** (`random-sites.ts` + `random-sites.test.ts`, which greps every `pRandom(` occurrence in `src/sim`): every M8 file with a draw site must add its line in the **same commit** (the test fails otherwise) — the ledger is the one shared file M8 lets several tasks edit, one key each (§2 note).
   - **`soundTraversed` exists in the sector SoA (`map.ts:199`); `soundtarget` does not** — M8-05 adds the field + its `resetOnLevelLoad` clear.
   - **In-tree today**: monster mobjs **already spawn and think** (spawnstate + `tics` desync + `totalkills++` + MF_SOLID blocking — `p_mobj.ts:593/609`), with A_Look/A_Chase dispatching to counted no-ops, so E1M1 currently contains ~27 frozen but solid monsters. M8 turns the actions on; it does not add spawning.
   - DEHACKED fullbright (A-02/D008, D002-is-env-facts) is **M8's task per ROADMAP** (M8-12); until it lands, fullbright frames (32773-style rows: monster fire frames, `S_BRBALL1`) render light-lit.
